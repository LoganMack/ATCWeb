# Alpha Touring Challenge — Website

Stack: [Astro](https://astro.build) (hybrid static/on-demand rendering) + [Tailwind CSS](https://tailwindcss.com) + [Supabase](https://supabase.com) (Postgres, queried via plain `fetch` against its REST API — no SDK dependency) + [Cloudflare Pages](https://pages.cloudflare.com) hosting.

Roster and news pages are rendered on-demand at Cloudflare's edge (`export const prerender = false`) with a 60-second cache, so editing data in Supabase shows up on the site within about a minute — no rebuild or redeploy required. The homepage's news teaser works the same way. Everything else is static.

## First-time setup

1. **Install dependencies**
   ```
   npm install
   ```

2. **Create a Supabase project** at supabase.com (free tier is enough for this). In the SQL editor, run, in order:
   - `supabase/migrations/0001_init.sql` — creates all tables, lookups, and RLS policies
   - `supabase/migrations/0002_auth_admin.sql` — adds `profiles`, admin-only write policies, `teams.status`, and the `logos`/`photos` Storage buckets (see "Auth & Admin Portal" below)
   - `supabase/migrations/0003_calendar.sql` — adds `circuits` and `events` (the race calendar — see "Calendar" below)
   - `supabase/migrations/0004_champions.sql` — adds `champion_photos` and public-read access to the results pipeline's tables (see "Champions, Standings & Race Results" below) — **only needed on a database that already has the results pipeline's tables** (`race_scores`, `curated_rounds`, `curated_race_results`, `curated_qualifying`); a brand-new Supabase project without that pipeline set up yet will fail on this migration's `alter table curated_rounds ...` line, since those tables won't exist. Skip it (or comment out the top section) until that pipeline is in place.
   - `supabase/migrations/0005_round_overrides.sql` — adds `round_overrides` (lets an admin flag a specific round as a non-points exhibition even inside a real championship season — see "Championships vs. exhibitions" below) and re-grants anon `SELECT` on `drivers`, codifying a manual fix applied directly in the SQL editor for a column-level grant gap on `drivers.iracing_cust_id`. Safe to run on any database regardless of whether the results pipeline's tables exist yet.
   - `supabase/migrations/0006_champion_photos_expand.sql` — widens `champion_photos.sort_order` from 3 slots (0-2) to 5 (0-4). Only needed if 0004 has already run on this database.
   - `supabase/migrations/0007_race_links.sql` — adds `race_links` (optional per-race iRacing results / replay / broadcast links — see "Per-race external links" below). Safe to run on any database regardless of whether the results pipeline's tables exist yet.
   - `supabase/migrations/0008_team_rosters.sql` — adds `team_rosters` (season-scoped team membership — see "Team rosters & race-results team logos" below). Safe to run on any database regardless of whether the results pipeline's tables exist yet.
   - `supabase/migrations/0009_car_logos.sql` — adds `car_logos` (admin-configurable car_name → logo lookup — see "Car logos" below). Safe to run on any database regardless of whether the results pipeline's tables exist yet.
   - `supabase/migrations/0010_circuit_layouts.sql` — adds `circuit_layouts` (per-circuit lap records — see "Circuit layouts" below). Safe to run on any database regardless of whether the results pipeline's tables exist yet.
   - `supabase/migrations/0011_driver_signup_date.sql` — adds `drivers.sign_up_date`. Safe to run on any database.
   - `supabase/migrations/0012_circuit_location.sql` — adds `circuits.location` (powers the public Circuits page — see below). Safe to run on any database.
   - `supabase/seed/seed_teams.sql`
   - `supabase/seed/seed_drivers.sql`
   - `supabase/seed/seed_news.sql`

   (`seed_teams.sql` and `seed_drivers.sql` are generated from the roster spreadsheet — see below.)

3. **Copy `.env.example` to `.env`** — already filled in with real values, nothing to look up.

4. **Run locally**
   ```
   npm run dev
   ```

5. **Deploy**: connect this repo in the Cloudflare dashboard under Workers & Pages → Create application → Pages tab → Import an existing Git repository (build command `npm run build`, output directory `dist`). Every `git push` to your main branch redeploys automatically. You don't need to add environment variables in the dashboard for this project — `wrangler.jsonc` already declares all four of them (see below) and is the single source of truth.

   Cloudflare's Git-connected builds now deploy via `wrangler deploy` rather than the older Pages-specific bundler, so `wrangler.jsonc` at the repo root (already included) is required — it tells Wrangler where the built worker (`dist/_worker.js/index.js`) and static assets (`dist/`) are. Bump `compatibility_date` in that file occasionally (any date is fine as long as it's in the past). A few things baked into this repo that fix errors Wrangler otherwise throws on deploy:
   - `public/.assetsignore` (copied into `dist/` on every build) tells Wrangler not to upload the `_worker.js` server bundle as a public static asset — it's still used as the Worker's entry point via `main`, just excluded from the public asset manifest.
   - `wrangler.jsonc`'s `vars` block declares all four `PUBLIC_*` values, including the Supabase anon key. This isn't a security shortcut: **once a `vars` block exists in `wrangler.jsonc` at all, Wrangler treats it as the complete set of runtime vars for the Worker and silently deletes anything set only in the Cloudflare dashboard that isn't also listed here** — that's what ate `PUBLIC_SUPABASE_ANON_KEY` on every deploy in earlier testing, regardless of whether it was typed as a Secret or plain Text on the dashboard side. Declaring it here instead of relying on the dashboard is what actually fixes it. This is safe specifically because a Supabase *anon* key is designed to be public and ship in client-side JS — it's not a secret, and access is controlled by the RLS policies in `supabase/migrations/0001_init.sql`, not by hiding this value. Don't put a genuinely sensitive key (a Supabase *service role* key, for example) in this file the same way.
   - If Wrangler ever warns about a Worker name mismatch, update `wrangler.jsonc`'s `"name"` field to match whatever your Cloudflare dashboard project is actually named.
   - **Why the roster/news actually failed to load in production, even after the fixes above:** `src/lib/supabase.ts` originally read `import.meta.env.PUBLIC_SUPABASE_URL` / `PUBLIC_SUPABASE_ANON_KEY` directly. `import.meta.env.PUBLIC_*` only gets a real value baked in at *build* time — but every page that calls Supabase has `export const prerender = false`, meaning it runs per-request on the deployed Worker, where `wrangler.jsonc`'s `vars` (or dashboard-bound variables) are only exposed via `Astro.locals.runtime.env`, not `import.meta.env`. The build was silently compiling with `undefined` every time, regardless of any dashboard/wrangler.jsonc configuration. Fixed by having `src/lib/supabase.ts` take the Supabase URL/key as an explicit parameter (`resolveSupabaseEnv(Astro.locals)`, checked in each page), preferring `Astro.locals.runtime.env` and only falling back to `import.meta.env` for contexts where that's the correct source (local `astro dev`, or a genuinely prerendered page).

## Auth & Admin Portal (v0.3)

The admin tools at `/admin` (publish news, edit drivers/teams, upload team logos, assign admin access) are gated behind a real login — no more "anyone with the anon key can technically read everything" being the only line of defense; writes now require an authenticated admin.

**Why email/password instead of "Login with iRacing":** the long-term plan is to authenticate through [iRacing's own OAuth](https://oauth.iracing.com/oauth2/book/) — its profile endpoint returns only `{ iracing_cust_id, iracing_name }` with no email, which is the cleanest way to handle EU privacy law for a series roster. iRacing has currently **paused new OAuth client registration**, so that integration can't be built yet. Everything is architected so it can be added later without reworking anything: `profiles` (added in `0002_auth_admin.sql`) is keyed to Supabase's own `auth.users.id`, not to email, and already carries `iracing_cust_id`/`iracing_name` columns that just sit `null` until that login method exists. In the meantime, Supabase's built-in email/password auth (its GoTrue REST API, called with plain `fetch` — same no-SDK approach as everything else in this repo) is what actually signs people in.

**Setting up the first admin:**
1. In the Supabase dashboard, go to Authentication → Users → Add user, and create an account with your own email and a password (or sign up through `/admin/login` once deployed — note the login page only signs *in*, it doesn't have its own signup form, specifically so random visitors can't self-register; creating the first account has to happen from the dashboard).
2. A `profiles` row is created for you automatically (role `driver`, via the `on_auth_user_created` trigger in `0002_auth_admin.sql`). Promote yourself to admin by running this in the SQL editor:
   ```sql
   update profiles set role = 'admin' where id = '<your-auth-user-uuid>';
   ```
   (Find your UUID on the same Authentication → Users screen.)
3. From then on, promoting anyone else is self-service: sign them into `/admin/login` once (which creates their `profiles` row), then use `/admin/users` to switch their role to Admin.

**How it works, if you're touching this code:**
- `src/lib/auth.ts` — the GoTrue REST calls (sign in, refresh, revoke, profile reads/writes) and the two auth cookie names/options.
- `src/middleware.ts` — runs on every on-demand request, resolves the session from cookies (silently refreshing an expired access token via the refresh token), and redirects anything under `/admin` to `/admin/login` unless the session belongs to an admin. Every `/admin/*` page can assume `Astro.locals.session` is a signed-in admin — the middleware already enforced it.
- All admin writes (`src/lib/supabase.ts`'s `create*`/`update*`/`delete*` functions) send the signed-in admin's own access token, never the anon key — Postgres Row Level Security in `0002_auth_admin.sql` is what actually allows or blocks the write. The app-layer gating in the middleware is a UX nicety; RLS is the real security boundary, same principle as the read-only policies from `0001_init.sql`.
- Team logo uploads go straight to Supabase Storage (`logos` bucket, public read / admin-only write) via `uploadToStorage()` in `src/lib/supabase.ts`.

## Sortable tables (v0.5)

Every data table on the site — the public Roster and Teams pages, and the admin Drivers/Teams/Events/Circuits/News/Users lists — can be sorted by clicking (or pressing Enter/Space on) any column header. This is one small shared script, `src/scripts/sortable-table.ts`, rather than a per-page reimplementation: any `<table data-sortable>` gets it automatically, as long as each sortable `<th>` has a `data-sort-key="foo"` and each row has a matching `<td data-col="foo">`.

By default a column sorts by its own displayed text (numeric text sorts numerically, everything else case-insensitively). When the sortable value isn't the same as what's displayed — a date that should sort chronologically rather than alphabetically, or the Roster's Penalty Points column — set `data-sort-value` on that `<td>` explicitly. Penalty Points is why this exists in the first place: it displays as e.g. "3/11", but the "/11" is just this season's point allowance (it's expected to change season to season), so `DriverRow.astro` sets `data-sort-value={driver.penalty_points}` on that cell — sorting only ever looks at the raw point count, never whatever the current allowance happens to be.

## Calendar (v0.4)

The nav's "Calendar" link and the homepage's "Upcoming Events" widget (right half of the hero) are both fed by `circuits` and `events`, added in `supabase/migrations/0003_calendar.sql`.

- **Circuits** (`/admin/circuits`) are a reusable reference table — a track gets raced across many seasons, so its name/logo only need to be entered once. Circuit logos are optional and upload to the same `logos` Storage bucket as team logos (see "Auth & Admin Portal" above); until you upload one, its slot in the calendar just shows a blank placeholder square. **Sourcing/uploading the actual circuit logos is left for later** — the admin UI and public pages both already handle a circuit having no logo gracefully.
- **Events** (`/admin/events`) are one row per race weekend: a circuit, an optional layout note (e.g. "Full Course" vs. a boot/chicane config — the same circuit can run different layouts from one event to the next, which is why `layout` lives on the event rather than the circuit), a date, and an overall format (Sprint / Endurance / Special). Every event can carry up to five sessions — Practice, Qualifying, Race 1 (required), Race 2 and Race 3 (both optional) — each with its own start time and a Dry/Mixed/Wet weather icon; Practice and Qualifying also track a length in minutes, and Qualifying/Race 1/Race 2/Race 3 also track a lap count. There's also an optional fuel-limit percentage and a link to the event's iRacing results page, both shown on the public calendar card when set.
- The public `/calendar` page lists every event (soonest first, with a separate dimmed "Past Events" section below), and the homepage hero shows just the next three, with the soonest rendered as a larger highlighted card including its full session breakdown.
- `src/lib/eventFormatting.ts` holds the shared date/time formatting and the `getEventSessions()` helper that flattens an event's five possible sessions into a display-ready list, skipping whichever of Race 2/Race 3/Practice/Qualifying weren't scheduled for that particular weekend.

## Champions, Standings & Race Results (v0.6)

Three public pages — `/champions`, `/standings`, `/results` (share a small tab bar, `HistoryTabs.astro`) — plus an admin section (`/admin/champions`) for uploading champion photos. **None of the underlying data is created by this repo.** `race_scores`, `curated_rounds`, `curated_race_results`, and `curated_qualifying` are populated by a separate, external iRacing-results import pipeline; `supabase/migrations/0004_champions.sql` only grants the anon key read access to them (they likely had no RLS configured at all before, since the import pipeline writes with a service-role key that bypasses RLS regardless) and adds the one table this repo does own, `champion_photos`.

All of the actual computation — season point totals, who's the champion, class-relative finishing position, win/podium/top-5/top-10 counts — lives in one file, `src/lib/results.ts`, specifically so there's a single place to fix if any of the business-rule assumptions below turn out to be wrong once real historical data is imported and you can eyeball the results:

- **Points**: `race_scores.total_points`, summed per round (`subsession_id` — i.e. race1+race2+race3 combined, when a round has more than one race), then the worst rounds are dropped before totaling. Drop count = **2 (baseline) + `seasons.extra_drop_weeks`** for that season. At least 1 round always counts even for a very short season, so a young class (Delta didn't exist before ATC5, Gamma before ATC16) can't get zeroed out by the drop-week math.
- **Class position** (wins/podiums/top 5s/top 10s): `race_scores.scored_position` is an *overall* field position across every class combined, not class-relative — so the per-class view doesn't use it directly (the new overall view does — see "Overall vs. per-class race results" below). Instead, `computeSeasonStandings()`/`getRoundResults()`'s per-class output re-rank each individual race themselves: take the drivers `race_scores` says were in this class for that race, look up each one's raw result in `curated_race_results`, and sort by `adjusted_position` (post-penalty) when set, falling back to `finish_position` (pre-penalty) otherwise. Whenever a row's `adjusted_position` differs from its `finish_position`, the results page shows an "Adj." badge so a penalty's effect on the result is visible, not just silently baked in.
- **Disqualifications**: a `race_scores` row with `dsq = true` is excluded from class-position ranking entirely (can never be a win/podium/top 5/top 10, and everyone behind ranks up normally) but still shows up on the race-results page, at the bottom, marked "DSQ" — and still counts toward that driver's own starts/appearances/points, since they did start the race.
- **Poles**: `race_scores.pole_bonus > 0`, counted once per round (`subsession_id`), not per individual race — races 2/3 run an inverted grid off race 1 and don't have their own qualifying, so they never earn a pole bonus. This is also why the Champions page's Poles stat shows "X% of **appearances**" while every other rate-based stat (wins, podiums, top 5s, top 10s) shows "X% of **starts**."
- **Starts vs. appearances**: a "start" is one `race_scores` row (one individual race); an "appearance" is one distinct round (`subsession_id`) — i.e. showing up for race1 only vs. also running race2/race3 are both 1 appearance, but 1 vs. up to 3 starts.

If any of this doesn't match how the league's scoring actually works once you can check it against real results, it's all contained in `src/lib/results.ts` — nothing on the page side needs to change to fix it.

The Champions page defaults to Alpha and lets you switch class via `?class=Gamma` links (a real page navigation, not a client-side toggle — computing a season's standings isn't free, so it only happens for the class you're actually looking at). Uploading a champion's (up to 5) photos happens at `/admin/champions` → pick a season/class → upload; the computed champion's name is shown for reference so it's obvious who you're uploading for. Each photo click-enlarges (`src/scripts/lightbox.ts`, shared with the public Champions page), and each upload/delete redirects back to the same page on success instead of re-rendering in place — the earlier version skipped that, which meant `hard-form-submit.ts` fell back to its fragile `document.write()` path and every photo upload felt like it required manually going back.

### Overall vs. per-class race results (v0.7)

A round's results page (`/results/[subsessionId]`) has two views, toggled at the top of the page:

- **Overall** (the default) — every class combined, ranked by `race_scores.scored_position`, which is what the points system is actually based on. Each row also shows which class that driver races in, color-coded the same way the Roster's class badge is (`src/lib/classColors.ts`, shared between the two).
- **Per Class** — the original view: each class gets its own ranked table, re-derived from `curated_race_results` the same way `computeSeasonStandings()` does (see above). No Class column here since every row in a given table is already the same class.

Both views are rendered by one shared component, `src/components/ResultsTable.astro`, with a fixed column order: **Pos, Driver / Number, Class (overall view only), Margin, Start, Incidents, Laps, Class Points**. There's no separate column for pole/penalty-adjustment anymore — those show as small inline markers instead: a gold "P" superscript next to Start for a pole, a gold "I" superscript next to Incidents when `finesse_bonus > 0` (the "3 incidents or less" bonus was actually awarded that race — same "driven by whether the bonus was awarded, not a re-derived raw threshold" approach as the pole marker), and a pink "*" superscript next to Pos for a post-penalty adjustment (hover for what it changed from). Every row is a fixed height (`h-8` wrappers around the Laps/Points cell contents) regardless of whether that row's subscript lines are actually present, so a table with a mix of "plain" and "detailed" rows still lines up cleanly.

**Margin** is the gap to the leader, formatted `xx.xxx` seconds, from `curated_race_results.interval_ten_thousandths` (`formatMargin()` in `src/lib/results.ts`) — the leader's own row (a 0.000 gap) shows "—" instead of "0.000", same as a missing interval. A *negative* interval is iRacing's way of flagging that a driver finished one or more laps down rather than showing a real (and misleading) time gap — that shows as **"-xL"** instead, where x is the leader's `laps_complete` minus this driver's.

**Result tags**: instead of a single hardcoded "disconnected"-style tag, each row carries a general-purpose `tags: string[]` (`RaceResultRow.tags`), rendered as small pills next to the driver's name — dynamic by design so a future tag (e.g. a penalty) can sit alongside without another schema/UI change. Today the only tag is **"Unclassified"**, shown whenever `race_scores.classified` is false — the results pipeline itself computes that (a driver who finished under 50% of the leader's laps doesn't score points and is unclassified); the app trusts that field rather than re-deriving the 50% threshold.

**Team**: each row also shows the team the driver raced for that race (`race_scores.team_id`) — its uploaded logo, or an acronym-of-initials placeholder tile when the team has no logo (`src/components/TeamLogo.astro` / `src/lib/teamLogo.ts`), same convention used on the Roster and Teams pages.

**Laps** shows `laps_complete`, with a small "N led" subscript underneath from `laps_led` when a driver led at least one lap. **Class Points** shows `race_scores.total_points` as the main figure, with a single combined "bonus" figure underneath — `class_points + finesse_bonus + pole_bonus + points_deduction` (everything in the total besides the base overall-position points), shown only when nonzero via `RaceResultRow.bonusPoints`. The full points system (base points by overall position, class points, the Sublime Finesse and Class Pole bonuses) has been in place since ATC16; a page showing that reference table for visitors, and a possible admin screen for configuring per-season bonus-point values (pole/finesse splits across a round's races, etc.), are later additions, not built yet.

Each race's table is labeled with a small "Race N" heading instead of that being baked into a table column header.

### Championships vs. exhibitions (v0.7)

Not every season/round on record counts toward standings and champions. Two independent mechanisms exclude non-points racing, both enforced inside `computeSeasonStandings()`/`getChampions()` so nothing on the page side can accidentally include one:

- **Season-level, name-derived, no schema needed**: a season only counts if its `name` matches `/^ATC\d+$/` (e.g. "ATC17") — anything else is treated as a non-points-paying/for-fun event. `isChampionshipSeason()` in `src/lib/results.ts` is the single source of truth for this; the `/results` and `/standings` season pickers use it too (`/results`' picker splits into "Championships"/"Exhibitions" `<optgroup>`s so both are still browsable, just clearly labeled — `/standings` only ever offers championship seasons, since an exhibition season would just show "no standings").
- **Round-level, admin-managed**: an individual round *inside* an otherwise-real championship season can also be a non-points exhibition — the concrete case that came up is a season's pre-season/first race run before points start counting. There's no existing column for this (and `curated_rounds` isn't a table this repo can alter — see the note on `supabase/migrations/0004_champions.sql`), so `0005_round_overrides.sql` adds a small sibling table, `round_overrides (subsession_id, is_exhibition, note)`. A signed-in admin sees a "Flag this round as an exhibition" / "Unflag" button at the bottom of that round's results page (visible only when signed in as admin); flagged rounds are excluded from every season's standings/champions computation via `getExhibitionRoundIds()`.
- **Displayed round numbers skip exhibitions entirely**: an exhibition round is labeled "Exhibition" instead of "Round N", and doesn't consume a number — flagging round 1 as an exhibition makes round 2 become "Round 1", rather than leaving a gap. `computeDisplayRoundNumbers()` in `src/lib/results.ts` recomputes this chronologically within a season every time, ignoring `curated_rounds.round_number` (the pipeline's own numbering, which doesn't know about exhibitions) entirely.

### Click-to-enlarge photos (v0.7)

`src/scripts/lightbox.ts` (loaded on every page via `Layout.astro`) turns any `<button data-lightbox data-lightbox-src="...">` into a click-to-enlarge trigger — a single delegated click listener opens a full-screen overlay with the full-size image, closable via the × button, clicking the backdrop, or Escape. Used on the public Champions page's photos and the admin champion-photo upload previews. The Champions page photo layout is one large photo (`aspect-video`, matching the expected 800×450 uploads, sized at 264px — 20% larger than the original 220px, to close more of the gap against the stats column) with 4 more shown smaller underneath in two rows of 2 (`0006_champion_photos_expand.sql` widened the slot limit from 3 to 5) — the second row exists mainly to give the photo column enough height to come closer to matching the stats column's height next to it, rather than leaving a lot of empty space at the bottom of the card. The card no longer shows a redundant "1st · Champion" badge next to the driver's name — the card's entire premise is that this driver *is* the champion, so it didn't add anything.

### Per-race external links (v0.7)

Each individual race (not round — a round with 3 races isn't necessarily 1 iRacing subsession) can have three independent, optional links, stored in `race_links` (`0007_race_links.sql`, keyed on `(subsession_id, race_number)`, same non-FK relationship to the results pipeline's tables as `round_overrides`):

- **iRacing Results** — built from a stored `iracing_subsession_id` via `iracingResultsUrl()` in `src/lib/results.ts`, rather than storing a full URL, so the link format only needs updating in one place if it ever changes. This is deliberately a *different* id than this app's own `subsession_id` grouping key.
- **Download Replay** — an external link (e.g. Google Drive) to the replay file. Replays are large, so this repo deliberately doesn't host them itself.
- **Watch Broadcast** — almost always a YouTube link.

All three show as small blue pill buttons (matching every other primary button/CTA on the site — `bg-brand-primary`) next to each race's "Race N" heading when set. A signed-in admin additionally sees an "Edit links" disclosure there (a `<details>` — no JS needed to open it) with a small form for all three fields, submitting via the same PRG pattern as everything else on this page. See `getRaceLinksForSubsession()`/`upsertRaceLinks()` in `src/lib/supabase.ts`.

### Team rosters & race-results team logos (v0.8)

`teams.id`/`drivers.id` are shared across every season, but a team's actual lineup changes every season — `team_rosters` (`0008_team_rosters.sql`) tracks that separately: `(season_id, team_id, driver_id)`, with two rules enforced by a database trigger (not just the admin UI, so a bad request can't bypass them): at most **4 drivers** per team per season (the format's cap is 3 today, but was 4 in earlier seasons — 4 is kept as the ceiling so older seasons stay representable), and a driver can only be on one team's roster per season. Manage it at `/admin/teams/[id]` → "Season Roster": pick a season, add/remove drivers from that team's roster for it.

The race-results team display (see above) deliberately reads `race_scores.team_id` directly rather than this roster table — that field already records the correct team for that specific race entry, no season lookup needed. The roster table exists as its own source of truth for "who was on this team's roster, season by season" (useful for a future team-history view), independent of any one race's data.

### Visual polish (v0.8)

Corner radius was reduced one step across the whole site's Tailwind scale (`rounded-2xl`→`rounded-xl`, `rounded-xl`→`rounded-lg`, `rounded-lg`→`rounded-md`, and the pill-shaped `rounded-full` buttons/tags→`rounded-lg`) — small logo/avatar images (plain `rounded`) were left as-is since they were already the smallest step on the scale. Radio/checkbox inputs (admin forms) now get `accent-color: theme('colors.brand.primary')` in `src/styles/global.css`, since without it browsers render their own default accent blue instead of the site's actual ATC blue (`#4369F5`, `brand.primary`/`brand.blue` in `tailwind.config.mjs`) — everywhere else on the site was already using the correct brand color.

## Overall vs. per-class season standings (v0.9)

`/standings` now has the same Overall / Per Class toggle as the race results page, defaulting to Overall. **Per Class** is the original behavior (`computeSeasonStandings()`, one class at a time). **Overall** is a new computation, `computeOverallSeasonStandings()` in `src/lib/results.ts`: every class combined into one table, scored with the same formula Alpha already effectively uses (`finish_points + finesse_bonus + pole_bonus + points_deduction`) — deliberately *excluding* `class_points`, Delta/Gamma's own per-race class-position bonus that Alpha never earns, so every class is judged on the same basis rather than Gamma/Delta having a structural leg up (Logan: "it should essentially be the alpha standings with everyone included"). Wins/podiums/top 5s/top 10s in this view come straight from `race_scores.scored_position` (the overall field position) rather than the per-class re-derivation the Per Class view uses. The Overall table adds a Class column so it's still clear which class each driver races in.

## Car logos & team/car logos on standings (v0.9)

Race results (and now standings) show the car a driver used as a small logo, the same way team logos work — `curated_race_results.car_name` is the per-race source (unlike `drivers.car`, which is just that driver's *current* car and isn't season/race-specific). `car_logos` (`0009_car_logos.sql`) is a simple admin-managed `car_name → logo_url` lookup; manage it at `/admin/car-logos`, which lists every car currently assigned to a driver (a practical stand-in for "every car in the data" — scanning the full historical results table for distinct names isn't practical over plain PostgREST) plus a manual "add a car by name" option for older cars. A car (or team) with no logo configured falls back to an acronym-of-initials placeholder tile, same as team logos — `src/components/TeamLogo.astro` is genuinely generic despite the name, used for both. Hovering any logo (team or car, placeholder or real) now shows its full name — previously only the placeholder tiles did that.

On the results page, both the team and car logo sit immediately to the right of the driver's car number. On the standings page, a driver can show *multiple* team and/or car logos if they raced under more than one that season — each logo's hover text is a small stat readout instead of just a name: a car logo says how many races it was used in; a team logo says how many races the driver raced under that team **and** how many of those races they were one of the team's top 2 point scorers in that specific race — the rule that decides whose points actually count toward the team championship that race. Both are computed once per season in `getSeasonCarTeamStats()`, independent of whichever standings view (Overall/Per Class) is currently showing.

## Circuit layouts (v0.9)

A circuit can be run in more than one configuration (full course vs. a shorter layout, etc.), each with its own lap record. `circuit_layouts` (`0010_circuit_layouts.sql`) is a normal child table of `circuits`: any number of layouts per circuit, each with a name, length (km), lap record, record holder, and record date. Manage them at `/admin/circuits/[id]` → "Layouts". Shown publicly at `/circuits` (see below); events still store their layout as free text (`events.layout`) rather than referencing a specific `circuit_layouts` row, so wiring that connection up is a reasonable next step, not done here.

**Lap record storage** (`0013_circuit_layout_lap_record_seconds.sql`): `lap_record_seconds` stores the record as a plain number of seconds to the nearest thousandth (e.g. `102.512`) — matching the format imported data actually comes in as — rather than a pre-formatted string. `formatLapTime()` in `src/lib/supabase.ts` converts that into the conventional "01:42.512" (`MM:SS.mmm`, zero-padded) for display; the admin form takes the raw seconds and shows a live "= 01:42.512" preview next to the field so entering it by hand doesn't require doing the minutes/seconds math yourself.

## Public Circuits page (v0.10)

`/circuits` is a fourth tab on `HistoryTabs` (Champions / Standings / Race Results / Circuits), listing every circuit with its logo, name, and location (`circuits.location`, `0012_circuit_location.sql` — free text like "Le Mans, France", nothing else in the app queries it structurally). Each row is a `<details>` — click anywhere on it to expand and show every one of that circuit's layouts (name, length, lap record + holder + date), no JS needed for the toggle. A circuit with no layouts recorded yet just shows "No results." when expanded.

## Generic "No results." empty states (v0.10)

Every "this list/table is empty" message across the site (admin list pages, the roster's status/class filters, results/standings/champions with nothing to show, the homepage's upcoming-events widget, etc.) now reads exactly **"No results."** — no more bespoke phrasing like "No champions on record yet for Alpha" or "No circuits yet — create one first." This does mean a couple of admin pages that used to point you at what to do next (e.g. "no circuits yet, create one first" on the New Event form) now just say "No results." instead — consistency was the explicit ask here over per-page helpfulness.

## Driver Roster tweaks (v0.10)

A few changes to `/roster` and its admin counterpart:
- The **Car** column is gone from the roster table — a driver can swap cars once a season without penalty, so "current car" isn't meaningful roster-wide (it still shows correctly per-race on Race Results, since that's sourced from `curated_race_results.car_name` for that specific race — see "Car logos" above).
- The team logo moved from next to the driver's name to next to the team name, in the Team column, where it visually belongs.
- The "X drivers currently on the books" subtitle is gone.
- Added a **Signed Up** column (`drivers.sign_up_date`, `0011_driver_signup_date.sql`), entered manually for now at `/admin/drivers/[id]`. This is laying groundwork for later: once drivers can sign up on the site themselves, this date (plus 90-day inactivity tracking and a "Veteran" exemption — 75+ appearances or an Alpha championship — that permanently protects a driver from ever being marked Inactive) is meant to drive automatic car-number release when an inactive driver's number becomes available again. None of that automation exists yet; only the column and its manual entry point do.

## Display formatting: dates, lap times, layout names (v0.12)

- **Dates** now display consistently as "Month D, YYYY" (e.g. "August 5, 2026") everywhere a date appears — driver sign-up date, circuit layout lap-record date, news post date, race round date. Centralized in a `formatDate()` helper in `src/lib/supabase.ts` (handles both plain `'YYYY-MM-DD'` columns and full ISO timestamps, and — same fix `formatEventDate()` already had — parses date-only strings as local calendar dates so the day never shifts back a day in negative-UTC-offset timezones). Event dates keep going through `formatEventDate()`/`formatEventDateShort()` in `src/lib/eventFormatting.ts`; `formatEventDate()` dropped the weekday-name prefix it used to include, to match the new site-wide format. `formatEventDateShort()` (the compact "Aug 15" used in the homepage's small upcoming-event teaser) was deliberately left alone — it's a tight-space abbreviation, not a full date, so it seemed like the wrong thing to force into "Month D, YYYY"; say the word if you'd rather it match too.
- **Lap times** no longer zero-pad the minutes digit — `formatLapTime()` now renders 102.512 seconds as "1:42.512" instead of "01:42.512" (seconds/milliseconds keep their fixed-width padding, since those are always exactly 2/3 digits).
- A layout named exactly "N/A" (the placeholder used when a circuit only has one configuration) now displays as **"Full Course"** on both the public Circuits page and the admin circuit editor's layout list — display-only via a new `displayLayoutName()` helper, the stored value is untouched, and the admin edit form's "Layout name" input still shows/edits the raw stored value.
- **Roster load errors** now show the actual error message (plus a hint about pending migrations) instead of a generic "check your env vars" line — same pattern as the Race Results page. If `/roster` was showing that generic message for you: the new error text will say what's actually wrong, but the most likely cause is `supabase/migrations/0011_driver_signup_date.sql` not having been run yet against your live database — PostgREST fails an entire query (not just one column) whenever any selected column doesn't exist, and `sign_up_date` was added to the roster query in v0.10.

## Post-race penalties (v0.13)

Implements rulebook 18.3's section 5 ("Stewarding") — logging a steward's penalty against a driver in a specific race, recalculating that race's results around it, and tracking the season-long penalty-points (PP) tally that drives probation. This is a big one; the pieces:

**Where the data lives.** `curated_race_results`/`race_scores` belong to the external iRacing-results import pipeline (see `src/lib/results.ts`'s header) — a re-import could silently overwrite anything written there. So penalties are their own app-owned tables instead (`penalties`, `penalty_offense_links`, `penalty_offenses` — `0014_penalties.sql`, same approach as `round_overrides`/`race_links`), and the app computes the resulting position/points adjustments on top of the imported data at read time, in `src/lib/penalties.ts`. A re-import can never lose penalty history this way.

**Logging a penalty.** On `/results/[subsessionId]`, admins see a small "+ Penalty" button next to each driver's name (rendered inline in the row, so it doesn't compete with the table's existing columns) that opens a dialog: Incident #, lap, description, time penalty (seconds), a flat points penalty (championship points — separate from any points lost indirectly from a time penalty repositioning the driver), penalty points (PP — separate again, the season-tally kind), and a multi-select of offenses. The offense list mirrors the rulebook's "Post-Race Penalties" table and is configurable at `/admin/penalty-offenses` — each offense carries a free-text "reference PP" (several rulebook entries are a range or an "or" choice, like "+1-4" or "Warning or 1 PP", that a single number can't represent) shown next to it in the dialog as a reminder; it's never auto-summed into the penalty's actual PP, which is always typed in by hand, per your call on how to handle that field.

**Recalculating the race.** Confirmed with you: a time penalty fully re-sorts the field (not just the penalized driver) — `applyPenaltiesToRoundResults()` adds the penalty to that driver's gap-to-leader and re-ranks everyone, so a driver who benefits from someone else's penalty gets their position, finish points, and class-points bonus recomputed too, exactly like a real post-race re-classification would. The two views (Overall/Per Class) share one underlying points total per driver, computed together so they never disagree. The position column shows the pre-penalty position struck through with the new one as a small subtext underneath. A flat points penalty just subtracts from that race's total, independent of any repositioning (rule 69 — a driver can end up net negative for the race). PP is entirely separate from race points; it only affects the driver's season tally, never this race's score.

**Documented simplifications** (both noted directly in `src/lib/penalties.ts`'s header): rule 64's "a big enough time penalty sends the driver a full lap down" conversion isn't modeled — the app doesn't have per-driver lap-time data to convert seconds into laps, so a penalty is only ever re-sorted within its existing lead-lap-or-laps-down group. And rules 58/62 (DSQ instead of a reset if the PP limit is hit in the season's final round; a 10-round ban for hitting it again mid-probation) aren't automated — there's nowhere in the app to track a served ban or an applied DSQ, so those stay a manual steward call.

**PP & probation (rules 57-62).** `drivers.penalty_points`/`penalty_points_max` already existed (default max 11, matching the rulebook); two new columns (`on_probation`, `probation_started_at`) track the rest. Saving a penalty with PP > 0 doubles it first if the driver's currently on probation (rule 60), adds it to their tally, and — confirmed with you — hard-resets to exactly 0 (discarding any overflow past the limit) and enters probation the moment the total reaches the limit. Probation lasts 4 rounds or 45 days from that date, whichever is longer — confirmed "4 rounds" means every scheduled calendar round, not just ones the driver starts, so `isOnProbationNow()` counts events rather than race appearances, computed fresh on every read rather than stored (so it self-resolves as the calendar moves, no cron job needed). The roster's Penalty Pts column shows a small "Probation" tag beneath the count while it's active. Both fields are also editable by hand on a driver's admin page, for correcting a mistake — deleting a penalty doesn't retroactively unwind its PP effect on the tally, so that's the way to fix it if a logged penalty turns out to be wrong.

**~~Incident Report link.~~ Removed in v0.16** — replaced by an actual in-app Incident Report page rather than an external link; see below.

**~~Known gap — season standings don't reflect penalties yet.~~ Closed in v0.15 — see below.**

### Penalty follow-ups (v0.14)

Five refinements on top of v0.13, all still working against the same app-owned tables (`0015_penalty_details.sql` adds two small pieces, described below):

**Editing an existing penalty.** Each logged penalty now has an "Edit" button alongside "Remove". It reopens the same dialog used to add one, pre-filled from the penalty's data (including its offenses and involved drivers), and posts to a new `update-penalty` action instead of `add-penalty`. ~~One deliberate asymmetry: editing a penalty's PP does not retroactively adjust the driver's season PP tally.~~ **Fixed in v0.15** — see below; editing (and deleting) now correctly recomputes the driver's season PP tally from scratch.

**~~Expand-to-reveal penalty display.~~ Removed in v0.16** — the click-to-expand row and its "+ Add Penalty" trigger are gone; see below.

**Involved cars.** The penalty dialog gained a checkbox list of the other drivers in that same race, for marking who else was part of an incident beyond the driver being penalized (`penalty_involved_drivers`, new join table, same shape as `penalty_offense_links`). Each penalty entry in the expanded panel shows an "Also involved: ..." line resolving those driver names. **Updated in v0.15**: the list now sorts by car number, excludes the driver the penalty is being applied to (their involvement is implied), and the "Also involved" line shows car numbers only, not names. **Updated again in v0.16**: renamed "Involved" and no longer excludes the penalized driver — see below.

**Warnings.** A penalty can now be flagged "this is a warning" (`penalties.is_warning`) rather than a real penalty — matching the several rulebook offenses that read "Warning or N PP" for a first offense, escalating to a real penalty on repeat. Warned entries show a small "Warning" badge in the panel (admin-only), and `getWarningCounts()` gives admins a running count next to the "+ Add Penalty" button so they can see when a driver's hit 2 warnings and the "2 warnings → +1 PP" offense applies. ~~One caveat: this count is career-wide, not scoped to the current season.~~ **Fixed in v0.15** — see below; it's season-scoped now.

**Position-subtext color.** The small subtext under a struck-through old position (showing where a driver ended up after re-sorting) is now color-coded instead of always pink: pink if the driver lost positions from their own penalty, blue if they gained positions because someone else was penalized ahead of them. Purely visual — the underlying recalculation logic from v0.13 is unchanged.

### Season-wide recalculation, appeals, and more penalty polish (v0.15)

**Season standings now reflect penalties — the v0.13 "known gap" is closed.** `computeSeasonStandings()`/`computeOverallSeasonStandings()` (standings and champions pages) used to total points straight from `race_scores`, unaware of anything in `penalties`. They now run the same recalculation engine `/results/[subsessionId]` uses, batched across every race in the whole season in one pass (`computeSeasonOverallAdjustments`/`computeSeasonClassAdjustments` in `src/lib/penalties.ts`, wired up via `getSeasonOverallContext` in `src/lib/results.ts`) rather than fetching one round at a time — fetching per round would mean an unacceptable number of extra requests on a page like Champions that shows many seasons at once. Only races that actually had a penalty logged are touched; everything else keeps its original `race_scores` values exactly as before, so this is a pure addition with no behavior change for a season with no penalties.

**Editing or deleting a penalty now correctly ripples through everywhere — including the driver's season PP tally.** The v0.13/v0.14 approach applied a penalty's PP forward once, at creation, and explicitly did *not* touch it again on edit or delete (flagged as a manual-correction caveat in both prior versions). v0.15 replaces that with `computeSeasonPPState()`: every time a penalty is created, edited, or deleted, the driver's whole season of penalties is replayed from scratch (chronological order, correct doubling-during-probation at each step) and the result is what gets saved — so there's no separate "undo" logic needed, and no more manual corrections for this specific case.

**Penalty points and warnings are now season-scoped, not career-scoped.** Both used to accumulate across a driver's entire history. Now:
- `getWarningCounts()` takes a list of subsession IDs and only counts warnings from those rounds — call sites pass the current season's rounds (`getCurrentSeasonRounds()`, new in `src/lib/results.ts`, reads whichever season has `is_current = true`).
- The PP tally computed by `computeSeasonPPState()` only replays the current season's penalties, matching the rulebook's own framing of PP as a *season* limit (rule 57).
- Probation itself is a deliberate exception — once triggered, it keeps running its own 4-round-or-45-day calendar clock (rule 61) independent of season boundaries, so a driver whose probation is still active from something that happened last season stays on probation this season. `computeSeasonPPState()` seeds its replay from the driver's actual `on_probation`/`probation_started_at` rather than resetting them, so a season with zero new penalties for that driver leaves an active probation untouched instead of silently clearing it.
- The roster's Penalty Pts column now live-computes each driver's current-season tally the same way (rather than trusting the stored `penalty_points` column, which only refreshes when one of that driver's penalties is actually mutated and could otherwise sit stale showing last season's leftover number) — same best-effort fallback pattern already used for the "Probation" tag.

**Appeals.** Editing a penalty now has an "This penalty was appealed" checkbox (`penalties.is_appealed`, `0016_penalty_appeals.sql`) that reveals an appeal result (free text) plus corrected time/points/PP fields. Once checked, every calculation in the app — this race's results, season standings, season PP — uses the appeal's values instead of the original ones (see the `effective*` helpers in `src/lib/penalties.ts`); the original logged values stay on record but stop being applied. An appealed penalty shows an "Appealed" badge and its result text in the expanded panel.

**Smaller polish, per Logan's request:**
- A penalty's description now renders on its own line below the incident/summary line, instead of run together in one paragraph.
- The "Penalty" tag is gold/yellow now (was the same neutral gray as other tags) and shows the headline number right on the tag — "Penalty (2PP)" or "Penalty (Warning)" — so admins don't have to expand a row just to see it.

**Documented simplification carried forward:** rules 58/62 (DSQ instead of reset if the PP limit is hit in the season's final round; a 10-round ban for hitting it again mid-probation) are still not automated — same reasoning as v0.13, nothing in the app tracks a served ban or an applied DSQ.

### Incident Report page & more penalty polish (v0.16)

**New Incident Report page.** `/results/[subsessionId]/incidents` replaces both the old external "Incident Report URL" link (v0.13) and the click-to-expand penalty panel on the results page (v0.14) with one real in-app page listing every incident logged for that round, reachable via a new "Incident Report" button next to the Overall/Per Class toggle. Includes **Position Δ** and **Points Δ** columns: each driver's change versus a "before" snapshot of that race, measured across the round's *full* penalty recalculation rather than any single entry in isolation (so two penalties against the same driver in the same race show the same delta, reflecting where they actually ended up). All penalty add/edit/remove now lives exclusively on this page — `/results/[subsessionId]` itself is read-only for penalties, showing only the static tag next to each driver's name. The shared server-side logic (season PP recompute, form field parsing) moved into `src/lib/penaltyActions.ts` so both pages could use it without duplicating it, back when the results page still needed it. **Its layout changed again in v0.17** — see below.

**"Racing Incident" wording.** A logged entry with no real effect — no PP, no warning, no nonzero time/points penalty (e.g. reviewed and cleared, or appealed down to nothing) — now shows as "Racing Incident" instead of "Penalty", both on the results-page tag and implicitly via the Incident Report's numeric columns reading blank/zero. A real penalty (any nonzero effect) still reads "Penalty", "Penalty (NPP)", or "Penalty (Warning)" as before.

**Appealed color.** The "Appealed" badge and its result text are pink now (`brand-pink`), not blue — freeing up blue for the position-gained indicator it's used for elsewhere on the results page.

### Incident Report layout & CSV import (v0.17)

**Broken apart per race, Race column dropped.** The Incident Report was one wide table for the whole round; it's now split into per-race sections (a "Race N" heading, then that race's incidents in their own table) — the same pattern the results page itself uses. A race with nothing logged just doesn't get a section.

**Narrower table, per Logan's request:**
- Driver now shows only the car number (no name) — narrower, and consistent with how "Involved" already showed numbers only.
- "Appealed" is no longer its own column — it's a small pink subtext under the driver's number; clicking it reveals/hides the appeal result inline rather than needing a whole column for it.
- "Warning" is no longer its own column — it's a small subtext under the PP figure.
- Edit/Remove moved out of an actions column into a slim row of their own directly below each incident (admin-only), left-aligned.
- Column order is now `#, Lap, Driver, Involved, Description, Offenses, PP, Time, Pts, Pos Δ, Pts Δ` — incident number leads (renamed from "Incident #" to just "#"), since it's the most natural way to identify a row at a glance.

**Collapsible descriptions.** Descriptions over 30 characters are hard-cut to exactly that length with a trailing ellipsis, behind a "Show more" toggle that swaps in the full text — multi-paragraph steward notes no longer blow out every row's height, or the table's width (an earlier CSS-`truncate` version could still stretch the row, since `truncate` needs a constrained width its flex layout wasn't giving it — a real character cap fixes that regardless of layout).

**Sorted by incident number.** Rows within each race sort by incident number (numeric; anything blank or non-numeric sorts last) instead of by driver car number.

**Offenses, one per line.** Previously a single comma-joined string; now each tagged offense gets its own line, easier to scan when an incident has two or three.

**CSV import.** Admins can bulk-import incidents from a CSV instead of one-at-a-time — an "Import from CSV" control next to "+ Add Incident" includes a downloadable template (`public/incident-report-template.csv`) and a file upload. Rows are matched by `race_number` and `driver_car_number` (not internal IDs, so the template stays human-editable); `offense_names` and `involved_car_numbers` are semicolon-separated lists resolved against this round's offenses/drivers. Unrecognized rows (bad race number, unknown car number) are skipped rather than failing the whole import, and the page reports how many rows imported vs. skipped after the upload. The parser (`src/lib/csv.ts`) is a small hand-rolled RFC4180-ish implementation (quoted fields, embedded commas/newlines, escaped `""`) rather than a dependency, since the format's simple enough not to need one.

**Dialog readability fix.** The Add/Edit Incident dialog's Race and Driver `<select>` dropdowns had unreadable white-on-white options in some browsers, since `<option>` elements mostly ignore a parent's background/opacity and fall back to browser defaults — fixed by setting an explicit dark background and white text directly on each `<option>`.

**"Involved" (was "Also involved"), and the penalized driver is included.** v0.15 excluded the driver being penalized from their own penalty's involved-cars checkbox list, on the reasoning that their involvement was implied. Per Logan, that's reversed: the Incident Report's picker no longer excludes anyone, so the penalized driver's own car can be tagged as "involved" alongside everyone else's, and the column/label is just "Involved" rather than "Also involved".

**Documented simplification carried forward:** same as v0.13 — rules 58/62 aren't automated.

## "View as Visitor" — admin impersonation preview (v0.18)

Lets a real admin click a toggle in the footer to preview the site exactly as a non-admin would see it — no admin-only buttons, edit links, or dialogs — without actually signing out, so UI changes can be assessed from a visitor's perspective without losing the admin session.

**How it works.** A new cookie (`atc_view_mode`, `src/lib/auth.ts`) holds `'visitor'` while the preview is active. `src/middleware.ts` computes two request-wide flags on every request (not just `/admin` ones, since the toggle itself renders on every page): `Astro.locals.isRealAdmin` (the actual role, untouched by the preview) and `Astro.locals.viewAsVisitor` (real admin *and* the cookie is set). A new helper, `isAdminView(Astro.locals)` in `src/lib/auth.ts`, is `true` only when both hold — every admin-gated page/component outside of `/admin` itself should call this instead of checking `session?.profile?.role === 'admin'` directly. Both places that actually needed it (`/results/[subsessionId]` and its Incident Report) were switched over; nowhere else outside `/admin` had admin-only UI to begin with.

**`/admin/*` itself is deliberately unaffected.** The middleware's existing route gate still keys off the real role, not `viewAsVisitor` — previewing the public site as a visitor should never lock a real admin out of the admin panel they're standing in. The Incident Report's POST handlers (add/edit/remove/import) do respect the preview though, since they gate on the same `isAdminView()`-derived `isAdmin` the page uses to decide whether to render those controls in the first place — belt-and-suspenders against a stale form somehow still submitting while previewing.

**The toggle.** Lives in `Footer.astro`, so it shows up on every page (including `/admin` ones, harmlessly) but stays out of the way — a small pill, gold when active. It deliberately checks `isRealAdmin`, not `isAdminView()`, so it stays visible and clickable to switch back even mid-preview. Submits to a new `POST /api/view-mode` (`src/pages/api/view-mode.ts`), which re-checks `isRealAdmin` server-side before touching the cookie (never trusts the form alone) and redirects back to whatever page the toggle was clicked from.

## News: round recaps, search, and season tags (v0.19)

Three additions to the news system, all from one request:

**1. Optionally link a post to a round — link at top, live recap at bottom.** A new "Linked round (optional)" dropdown on the New/Edit Post admin forms (grouped by season, newest first) sets `news_posts.round_subsession_id` (`supabase/migrations/0017_news_round_season.sql`; deliberately no foreign key to `curated_rounds`, same reasoning as `race_links`/`round_overrides` — that table belongs to the external results-import pipeline). When a post has a linked round, the article page shows a "View race results — {track} →" button at the top, and a full Race Recap section at the bottom: for each race, top 3 finishers per class, top 3 rookies, and the fastest lap (with a "New Track Record" badge when it beats the circuit's logged lap record); then which team scored the most points overall and which scored the most within the Delta class specifically (Alpha/Gamma/Delta are the site's three driver classes — Delta and Gamma both carry their own bonus class-points total distinct from the overall score, so "top Delta team" means top by Delta-class points, not top Delta-class driver); then a "Who Scored For Who" breakdown of every scoring driver by team and race.

The recap is **computed live on every page view** (`src/lib/newsRecap.ts`'s `computeRoundRecap()`) — nothing about it is stored as text. That means it can never go stale: if a penalty gets logged against that round's results after the article is published, the recap picks up the recalculated positions/points automatically on the next view, same as the results page itself does.

Fastest lap is the one part of the recap sourced outside the normal results pipeline — it reads `curated_race_results.best_lap_ten_thousandths` directly, in its own isolated, try/catch-wrapped query (`fetchBestLaps()` in `newsRecap.ts`) that just omits the stat on any failure. That isolation is deliberate: `results.ts`'s shared query (`getCuratedRaceResultsForSubsessions`) backs every results/standings/champions page on the site, and PostgREST fails an entire query if even one selected column doesn't exist on the table — so threading an unverified column name through that shared function would have put the whole site's results pages at risk over one recap stat. Track-record comparison matches a round's `track_name` directly against `circuits.name` (per Logan, track names are consistent everywhere, so this isn't a fuzzy guess), disambiguating by a new `curated_rounds.layout` column (`0018_curated_rounds_layout.sql` — a deliberate, documented one-off exception to this repo never altering the external pipeline's own tables, since Logan populates it himself) only when a circuit has more than one layout on file. When it can't resolve to exactly one layout, the recap just shows the lap time with no record badge, plus an admin-only on-page notice (via `isAdminView`) explaining why, so Logan knows what to add in Admin → Circuits — regular visitors never see it.

**2. Title search.** The public News page (`/news`) now has a search box that filters posts by title (case-insensitive substring match), as a `?q=` query param — plain GET so results are linkable/shareable.

**3. Season tags.** Posts can be tagged with a season while editing — a free-text field (`news_posts.season_label`, same migration as above) with autocomplete suggestions drawn from every season label already in use across posts and rounds, rather than a locked-down dropdown. Free text on purpose, per Logan's request: it needed to cover non-championship/exhibition seasons too, which don't necessarily have their own `seasons` table row, and a plain text tag is easier to visually scan and filter by than forcing everything through a rigid picker. The News page grew a matching "Season" dropdown filter (`?season=`, combinable with the search box), the admin News list grew a sortable Season column, and both `NewsCard` and the article page show the tag as a small badge.

**Documented simplification carried forward:** same as v0.13 — rules 58/62 aren't automated.

## News recap polish + historical team logos (v0.20)

Five small follow-ups to v0.19's news round-recap, plus one bigger addition:

**1. "View Race Results" button no longer includes the track name** — it's already named in the article's own recap heading right below, so the button text is now just "View Race Results →".

**2. Team logos instead of team names in the recap.** Next to each driver's name in the per-race top-3-by-class lists, the team's logo now shows instead of a text name (matching the convention every results/standings table on the site already uses — `TeamLogo`, hover for the full name). In the "Most Team Points" cards and the Team Scoring Breakdown, the logo now shows alongside the team name rather than replacing it, per Logan.

**4. "Who Scored For Who" → "Team Scoring Breakdown", now collapsible** — a native `<details>`/`<summary>` (no JS needed), open by default.

**5. Team points card titles now say "team".** "Most Points — Overall/Delta" → "Most Team Points — Overall/Delta", so it reads unambiguously as a team stat next to the driver-focused stats above it.

**3. Historical team logos, per season.** Teams have rebranded across ATC's history, and results/standings/news for a past season should show what a team looked like *then*. New "Historical Logos" section on the admin team edit page (`/admin/teams/[id]`): pick a season and upload a logo for it — a completely separate upload from the team's current logo above (different storage paths, different table), so setting a new current logo can never overwrite a season's historical one, and vice versa. A season with nothing uploaded here just falls back to the team's current logo.

Backing table: `team_season_logos` (`0019_team_season_logos.sql`), one optional row per (team, season), admin-write/public-read RLS matching every other admin-managed table in this repo. `src/lib/results.ts`'s `resolveTeamLogo()` is the one place that resolves "this team's logo, as of this season" (override if one exists, else the current logo) — wired into `getRoundResults()` (so a round's results/standings AND the news recap built on top of it automatically show the right-era logo) and `getSeasonCarTeamStats()` (the standings page's per-driver team-usage rows). Because that lookup is now load-bearing for every results/standings page rather than an isolated add-on stat, it's fetched through a try/catch wrapper (`getAllTeamSeasonLogosSafe`) — if `0019_team_season_logos.sql` hasn't been run yet on the live database, every page just falls back to showing current logos everywhere (today's behavior) instead of failing to load at all.

## News cover images move to Storage + post backdating (v0.21)

Two admin workflow improvements to news posts:

**Cover images now upload directly, instead of a manually-typed URL.** The New/Edit Post admin forms' "Cover image URL" text field is now a real file upload (PNG/JPG/WebP/SVG), going straight to Supabase Storage's `photos` bucket — the same bucket champion and driver photos already use — instead of pointing at a static file checked into `public/images/news/` in the repo. That old workflow meant adding a cover image required committing a file and redeploying; now it's just an upload from the post editor, live immediately. The edit page shows the current image with the upload control, plus a "Remove the current cover image" checkbox for clearing it outright without replacing it. Uploading a new file never touches any other post's image — each upload's storage path is keyed off that post's slug plus a timestamp. `news_posts.cover_image_url` itself is unchanged (still just a URL column) — only where that URL comes from changed, so any older post seeded with a static repo path (e.g. the ATC17 champion photo from `seed_news.sql`) keeps working exactly as before until it's re-uploaded.

**Posts can be backdated.** Both admin forms gained a "Published date" field (defaults to today on a new post, pre-filled with the post's current date on edit). It fully replaces the old implicit behavior where saving a draft-to-published transition silently bumped `published_at` to the current moment — that bump is gone; the date field is now the one source of truth, so picking an earlier date (e.g. writing up an older race after the fact) backdates the post, and it sorts into the News page and admin list in the right chronological spot. Stored at noon UTC for whatever date is picked (not midnight) so it can't render as the previous day once `formatDate()` shows it in a viewer's local, negative-UTC-offset timezone.

## Rule 18.3.2: penalties against laps-down drivers, and expandable result rows (v0.22)

Three related changes to the results/penalty engine, all driven by rulebook 18.3.1/18.3.2.

**The gap this closes.** A time penalty against a driver still on the lead lap always worked — it's added to their real, exact gap-to-leader (`interval_ten_thousandths`, iRacing's own telemetry) and the field re-sorts around that. A driver already a lap or more down has no such number to work with (iRacing reports a negative "-xL" flag there instead of a real time), so a penalty against a laps-down driver used to just... do nothing. Per Logan, the fix is the same one used by hand before this was automated: estimate each driver's own "overall race time" as their own completed laps × their own average lap pace, and use *that* to compare and re-rank laps-down drivers against each other and against the penalty. This also directly implements the other half of rule 18.3.2 — a large enough penalty against a lead-lap driver now correctly crosses them into (or further into) laps-down territory, using the leader's own average lap as the stand-in for "the leader's final lap pace" (the pipeline doesn't capture a literal last-lap time).

**How the estimate is used.** `reorderByTimePenalty()` (`src/lib/penalties.ts`) now prefers exact data wherever it exists — an untouched row keeps its real interval/laps-down bucket exactly as before, and a penalized lead-lap driver's new gap is still exact arithmetic (real interval + penalty seconds). The average-lap estimate is only reached for the case with no exact number to fall back on: a driver already laps down who gets a further penalty. A penalty is also guaranteed never to *reduce* a driver's laps-down count — the estimate is clamped against what the pipeline already recorded. Missing average-lap data for a given row (or the leader) just leaves that row's laps-down bucket where it was; the penalty's points/PP effects still apply as before, only the re-ranking is skipped for lack of data to rank it by. This average-lap data comes from a new isolated query (`getLapStatsForSubsessions`, `curated_race_results.average_lap_ten_thousandths`/`best_lap_ten_thousandths`) — same defensive pattern as every other pipeline-column read in this app: if that column's ever missing or renamed upstream, only this feature degrades, not the whole results/standings/champions pages.

**"Overall race time" is now a real, visible number, not just an internal comparison tool.** Every result row now carries `overallRaceTimeFormatted`/`overallRaceTimeTenThousandths` (laps × average lap, reflecting any penalty already added in) alongside new `averageLapFormatted`/`bestLapFormatted` fields — computed live as part of the normal results query (consistent with how every other derived result value in this app works — nothing new is written to the database; a page re-render always reflects the latest penalties/imports with no risk of a stale stored value). These are exposed in the results table's new expandable row detail (next paragraph).

**Rule 18.3.1's classification/points threshold was already correct** — "drivers who finish under 50% of the leader's laps are unclassified and score no points, otherwise they earn at least the minimum points" was already fully handled upstream (`race_scores.classified`, `CLASSIFIED_FALLBACK_POINTS` in `src/lib/penalties.ts`) before this version; nothing needed to change there.

**Result rows are now click-to-expand.** Clicking anywhere on a driver's row in the results table (`src/components/ResultsTable.astro`) reveals a detail panel with their average lap, best lap, overall race time, laps led, and a fully itemized points breakdown (finish/class/finesse/pole/deduction, each shown only when nonzero, plus the total) — pulling laps-led and the combined bonus-points figure out of the row's own subtext in the process, so the collapsed row is back to a single fixed-height line (same visual density as before all the bonus/laps-led subtext was added). Purely a display change — no scoring or penalty logic lives in this component.

**Bug fix (v0.22.1):** the laps-down estimate above had a sort bug — any row with a computed estimate (i.e. one that received a penalty) was treated as unconditionally better than every untouched laps-down peer, regardless of actual size, so a small penalty against a laps-down driver could vault them straight to the front of the whole laps-down group. Fixed by computing the same estimate for every row in a laps-down bucket (touched or not) so they're fairly comparable by magnitude, while still protecting two untouched rows from ever reordering relative to each other off of the estimate's own noise.

## Racing Incident penalty entries, and a clearer points breakdown (v0.23)

**An incident can now be logged with no driver at fault.** The add/edit dialog's Driver field (Incident Report page) gained a "Racing Incident (no driver at fault)" option alongside the real drivers. `penalties.driver_id` is now nullable (`0020_penalty_racing_incident.sql`) — null means exactly this: reviewed and judged nobody's fault, so nothing is attached to a specific car. It shows as "RI" in the Driver column instead of a car number. A driverless entry can never affect anyone's position or points — the whole recalculation engine (`src/lib/penalties.ts`) keys everything off driver_id, so a row with none simply isn't attributable to any driver's result; that's the intended behavior for a no-fault call, not a gap. The offense checklist and "this is a warning" checkbox — both of which only make sense when assigning fault — are hidden (and cleared, so nothing stale gets submitted) whenever "Racing Incident" is selected, live-updating as the Driver field changes; the "Involved" car list stays available either way, since who was involved is independent of who, if anyone, was at fault.

**The expanded row's points breakdown now shows the net effect of this round's penalties.** Alongside the itemized finish/class/finesse/pole/deduction breakdown added in v0.22, a driver whose points were changed by a penalty — their own, or a cascade from someone else's in the same race repositioning them — now gets a colored callout ("+8 pts" / "−12 pts from this round's penalties") showing exactly how many points they gained or lost, rather than leaving it to be inferred from the breakdown's components. Backed by a new `RaceResultRow.originalTotalPoints` field: the pipeline's pre-penalty total, carried alongside the (possibly-adjusted) `totalPoints` so the delta between them is always available without a second query.

## Six site-wide fixes: penalty margins, logo order, circuit images, and driver name display (v0.24)

**Leader-penalty margin edge case fixed.** If the race winner is penalized but still finishes far enough ahead to keep the win, their margin now stays at 0 (`—`) as expected, and everyone else's margin shifts to reflect the gap to that still-winning leader — instead of the old behavior, which added the penalty time straight onto a margin of 0 and displayed that as the leader's own gap, while leaving the rest of the field's margins untouched (stale, pre-penalty numbers). The fix is a re-zeroing step in `reorderByTimePenalty()` (`src/lib/penalties.ts`): once every lead-lap driver's adjusted time is computed, the whole group is re-based by subtracting whichever adjusted time is now actually smallest, so the real leader is guaranteed to show 0 no matter whose penalty caused the shuffle. `recomputeRow()` was also changed to refresh a row's margin/tags whenever the leader's reference point moves — not just when that specific row has its own penalty, which was the root cause of untouched rows never getting the new margin. This also now correctly handles the more severe case where a leader's penalty is large enough to hand the win to someone else (the new leader shows 0, the old leader shows their new deficit).

**Car logo before team logo, everywhere both appear.** Every driver has a car, but not every driver has a team, so the car logo now renders first with the team logo (if any) after it — swapped in the results table (`src/components/ResultsTable.astro`), season standings (`src/pages/standings.astro`), and the new Champions page car/team display added below.

**Circuit layouts now have their own images.** Previously every layout at a circuit shared one image (the circuit's own logo). `circuit_layouts` gained an `image_url` column (`0021_circuit_layout_image.sql`); the admin circuit page (`/admin/circuits/[id]`) has an image upload on both the add-layout and edit-layout forms, and the public Circuits page (`/circuits`) shows each layout's own image next to its lap record. A layout with no image of its own still falls back to the circuit's shared logo (`layoutImageUrl()` in `src/lib/supabase.ts`), so nothing regresses for circuits that haven't uploaded per-layout images yet.

**"N/A" layouts now display as "Full Course".** This was already handled by an existing `displayLayoutName()` helper (`src/lib/supabase.ts`), already wired into every layout-name render site (public Circuits page and the admin circuit detail page) — verified as part of this pass rather than needing new work.

**Driver names no longer show iRacing's duplicate-name disambiguation number.** iRacing appends a trailing number directly onto a driver's stored name when two real people share the same name (e.g. "Chris Macdonald5") — see the `drivers.name` column comment in `0001_init.sql`. A new `displayDriverName()` helper (`src/lib/supabase.ts`) strips a trailing run of digits for display only; the stored `name` value is never touched, keeping the database aligned with iRacing. Applied everywhere a driver's name renders on public pages — roster (`DriverRow.astro`), results (`ResultsTable.astro`), standings, Champions, and news recaps — plus the admin Champions list/detail pages. Left untouched, deliberately: the admin driver list/edit form (needs the real stored value to edit it) and the two admin driver-picker `<select>`s on the Incident Report page and a team's roster-management page, where the raw number is the only thing that disambiguates two drivers who'd otherwise show up as identical names in a dropdown.

**Champions page now shows each champion's car(s) and team(s) for that season.** Reuses the existing `getSeasonCarTeamStats()` (already powering the same display on the Standings page) rather than adding new data-fetching logic — `champions.astro` now fetches it per champion season and passes the relevant driver's `DriverSeasonExtras` into `ChampionCard.astro`, which renders the same car-then-team logo row (with race-count tooltips) next to the champion's name. Scoped to the public Champions page only — the admin photo-management page (`/admin/champions`) still calls the plain `getChampions()` with no extra queries, since it has no use for this data.

## Three scoring-accuracy fixes: Alpha class points, drop weeks, and overall poles (v0.25)

**A penalty-driven podium shuffle could no longer manufacture Class Points for an Alpha driver.** Alpha's own scoring never includes the top-3-in-class "Class Points" bonus — that's Gamma/Delta's own per-race class-position bonus (see v0.13/v0.24's notes above), and the original `race_scores` data reflects that correctly. But the penalty-recompute engine (`src/lib/penalties.ts`) only ever consulted a class-position → points lookup table keyed by format (endurance/sprint), with no awareness of which *class* it was computing for — so if a penalty against another driver moved an Alpha driver onto the class podium, the recompute wrongly awarded them Class Points the original pipeline data would never have given them. `classPointsForPosition()`, `recomputeRow()`, `recomputeScorePoints()`, and `computeSeasonClassAdjustments()` all now take an `awardsClassPoints` flag (resolved from the driver's actual class name, `!== 'Alpha'`), threaded through from every call site — the results page, the Incident Report page, season standings, and the news recap's linked-round display — so a class that never earns this bonus can't have it manufactured for it by a penalty recalculation, no matter how positions shuffle.

**Drop weeks now correctly include rounds a driver scored zero in or missed entirely.** The worst-N-rounds-dropped math (`finalizeStandings()` in `src/lib/results.ts`) built its pool of "this driver's rounds" purely from `race_scores` rows that actually existed for that driver — a round they scored 0 in still had a row (so it was already counted correctly), but a round they didn't show up for at all had *no* row, so it silently vanished from the pool instead of being an available (and, being worth 0, likely-dropped) week. The baseline 2-round drop (plus any `season.extra_drop_weeks`) then applied against that artificially small pool, meaning missed rounds got "dropped" for free while the drop count *also* ate into real scored rounds on top of that — a driver could lose credit for legitimate races even though the intent was only ever to drop their worst weeks, no-shows included. `finalizeStandings()` now takes the full set of `subsessionId`s the season (or, for the per-class view, the class) actually had, and pads every driver's round-points pool out to that complete set with explicit zeros for anything missing before sorting and dropping — so a driver with two no-show weeks and a 2-round drop allowance gets exactly those two zeros dropped, and every real result they posted counts.

**The Overall standings' Poles stat now counts actual overall poles, not each class's own "Class Pole."** `race_scores.pole_bonus` is Gamma/Delta's own per-race "Class Pole" bonus — fastest qualifier within just that driver's class, not necessarily the fastest on the whole combined grid (see the Points System note in v0.13's own README section). The Per Class standings view is right to use it as-is (a class's own pole is exactly what that view should show), but `computeOverallSeasonStandings()` was using the same column for its cross-class table, meaning a Gamma or Delta driver who merely out-qualified their own class — without ever being the fastest car on track — still showed up with a "pole" in the all-classes-combined view. That view now derives the real pole-sitter directly from `curated_race_results.starting_position` on each round's race 1 (the only race with real qualifying — races 2/3 invert off it and never earn `pole_bonus` at all, for the same reason), using data the view already fetches for its penalty recalculation, so no extra query was needed.

## Team scoring fixes, new recap breakdowns, and Team Standings (v0.26)

**Only a team's top 2 scoring drivers in a race now ever count toward that team's points.** The news recap's team-points math (`src/lib/newsRecap.ts`) summed every driver on a team into that team's total for a race, regardless of how many the team fielded — so a team running 3 drivers in the same race could get credit for all 3, when the rule has always been "only the top 2 scoring drivers for each team add their points to the team championship per race" (the same rule `getSeasonCarTeamStats()` in `src/lib/results.ts` already correctly implemented for the Standings page's little team-logo tooltips). That rule is now a single shared helper, `topTeamScorers()` in `src/lib/results.ts`, used everywhere team points are computed — the news recap, the results page's new per-driver "Team Points" detail, and the new Team Standings page below — so there's exactly one place this logic lives and every view agrees with the others. The "overall" (cross-class) team competition ranks and sums by the same class-blind formula `computeOverallSeasonStandings` uses (finish + finesse + pole + deduction, excluding class_points) so a Gamma/Delta driver's own class bonus can't give their team a structural edge; Delta's own separate team competition (below) deliberately keeps class_points in, since that's the whole point of tracking it separately.

**Three new per-round breakdowns on the news recap.** Alongside the existing (now-fixed) "Team Scoring Breakdown," a round's recap (`/news/[slug]`, when linked to a round) now also shows: a **Delta Team Scoring Breakdown**, the same idea scoped to just Delta-class rows and ranked/summed by each row's full total points (Delta's own class_points bonus included, matching the existing `topTeamDelta` figure it sits alongside); a **Sublime Finesse** list per race, every driver actually awarded that race's "3 incidents or less" bonus; and a **Naked Aggression** list per race, the top 5 position gainers (raw starting position minus raw finish position, whole field, every class combined) — who actually drove through the field hardest on track, independent of any later stewarding.

**The results page's expanded row detail now shows "Team Points."** Alongside the existing average/best lap, overall race time, and laps-led stats, a driver with a team now also sees how many points they actually contributed toward their team's total for that specific race — 0 if they weren't one of the team's top 2 scorers that race, computed the same way (and guaranteed to agree with) the news recap's team breakdown and the new Team Standings page.

**New Team Standings page** (`/team-standings`, added to the History sub-nav alongside Champions/Standings/Race Results/Circuits). Same concept as the driver Standings page — pick a season, see a final ranked table — but for teams: each team's points are the sum of its top-2-scorers-per-race totals across the season, with the same worst-rounds-dropped rule (baseline 2 + `season.extra_drop_weeks`, at least 1 round always counted) driver standings already use, applied to the team's own per-round total instead of a driver's. `computeTeamSeasonStandings()` in `src/lib/results.ts` is the new function backing it.

## Recap breakdown reorganization, Team Standings polish, full tiebreakers, and expandable standings rows (v0.27)

**Sublime Finesse and Naked Aggression moved out of the per-race cards into their own collapsible sections, and all 4 breakdowns now collapse by default.** Previously these two lists rendered inline inside each race's own result card on the news recap (`/news/[slug]`); they're now their own top-level `<details>` sections (grouped by race, same card-per-group layout the Team Scoring Breakdown already used), sitting alongside Team Scoring Breakdown and Delta Team Scoring Breakdown. The underlying data didn't change — `newsRecap.ts`'s per-race `sublimeFinesse`/`nakedAggression` arrays are unchanged — only where they render. The Team Scoring Breakdown's `<details open>` also lost its `open` attribute, so all four breakdown sections are now collapsed by default rather than just three of them.

**Team Standings' subtitle removed** — the explanatory paragraph about how team points are calculated is gone from `/team-standings`; the page speaks for itself via its columns.

**Team Standings rows now expand to show the roster.** Clicking a team (same click-to-expand pattern as the results table and standings page — see below) reveals every driver who raced for that team this season, each with their overall championship position, season points, and starts, pulled from `computeOverallSeasonStandings()` and joined in at the page level. `computeTeamSeasonStandings()` now also returns each team's `driverIds`.

**Season standings now use a full tiebreaker chain instead of stopping at wins/podiums.** Per Logan: "wins, podiums, top 5s, top 10s, poles, laps led, laps, and appearances. If they are still tied, alphabetical order." `finalizeStandings()` in `src/lib/results.ts` now sorts through that entire chain, falling back to alphabetical-by-display-name (disambiguation digits stripped, matching how names actually show on the page) only if every column above is exactly tied. `DriverSeasonStanding` gained `lapsLed` and `laps` season-total fields to support this. For the Gamma/Delta per-class views, wins/podiums/top5s/top10s/poles were already that class's own class-relative stats (and Class Pole for poles) — nothing needed to change there. For Alpha, whose own `pole_bonus` is structurally always 0 (Alpha never earns Gamma/Delta's "Class Pole" bonus), both the *displayed* Poles column and the poles tiebreaker on Alpha's per-class view now derive the real overall pole-sitter the same way the Overall view already did (`curated_race_results.starting_position` on each round's race 1), via a newly-shared `computeOverallPoleDriverBySubsession()` helper — consistent with Logan's own wording grouping "overall and alpha" as behaving the same way.

**Standings rows are now expandable**, same click-to-expand interaction the results table and Team Standings use. A driver's expanded detail shows: laps, laps led, net positions gained/lost (season total of starting position minus final position, using the same penalty-adjusted position everywhere else in the app uses), incidents per lap, season penalty points (PP — the appeal-aware rule-57 counter, distinct from a race's points deduction), bonus points (finesse + pole bonuses summed; deliberately excludes Gamma/Delta's own class_points, matching how the Overall standings already treats that bonus as not part of the cross-class competition), and total distance driven in km (laps completed × that round's matched circuit-layout length, summed across the season — a round that can't be matched to a `circuit_layouts` row via the same track/layout-name matching the news recap's track-record check uses just doesn't count toward that total, so a driver with some untracked rounds still shows a real, if slightly conservative, figure). All of this is computed by a new `getSeasonDriverExtendedStats()` in `src/lib/results.ts`.

**Not implemented — "corners per incident":** Logan's original list also asked for a corners-per-incident stat. Nothing in this schema currently tracks a circuit layout's corner count (`circuit_layouts` has no such column), so there's no data to compute this from. It's left out of the expanded row entirely rather than shown as a fake or permanently-empty stat; adding it would need a new `circuit_layouts` column (similar to how `image_url` was added in `0021_circuit_layout_image.sql`) plus an admin UI field to fill it in per layout.

## Circuit layout corner counts, and "corners per incident" (v0.28)

**Circuit layouts can now have a corner count.** `circuit_layouts` gained a `corners` column (`0022_circuit_layout_corners.sql`, nullable — existing layouts start unset). The admin circuit page (`/admin/circuits/[id]`) has a "Corners" input alongside "Length (km)" on both the add-layout and edit-layout forms, and each layout's summary line shows it once set (e.g. "4.088 km · 12 corners").

**Standings' driver detail panel now shows "Corners / Incident."** This was left out of v0.27's expanded row because nothing in the schema tracked a layout's corner count at all — now that it can be entered, `getSeasonDriverExtendedStats()` in `src/lib/results.ts` computes it the same way it already computes distance driven: each round's `track_name` (+ `curated_rounds.layout` for a multi-layout circuit) resolves to a `circuit_layouts` row via the same direct/normalized matching newsRecap.ts's track-record check uses (`resolveLayout()`, replacing the narrower `resolveLayoutKm()` from v0.27 so both distance and corners share one lookup), corners × laps completed gives that round's corners navigated, summed across the season and divided by total incidents. Shows as "—" rather than a number in two cases: this driver had zero incidents (an undefined/infinite ratio, not a real value), or none of their rounds this season resolved to a layout with a corner count on file yet. Like distance driven, a driver with only some rounds tracked still gets a real (if conservative) figure from the ones that are.

## Roster "Unassigned" filter, remaining "N/A" layout display, and a Calendar-grid view (v0.29)

**Roster page gained an "Unassigned" filter.** Alongside Active/Inactive/All, a new "Unassigned" button in the status filter group shows every driver with no current car number who has raced before (`car_number === null && appearances > 0`) — regardless of their actual roster status, since a numberless returning driver could be flagged Active, Inactive, or anything else. Pure frontend addition: `getDrivers()` already returns every driver row with both fields, so `DriverRow.astro` just gained a `data-unassigned` attribute and roster.astro's filter script's `statusMatches()` special-cases this one filter to ignore status entirely and check that attribute instead.

**Fixed the last "N/A" layout display gap.** The v0.24 `displayLayoutName()` fix ("N/A" → "Full Course") had been wired into every layout-name render site except two: `EventDetailCard.astro` (the Calendar page's event cards) and `UpcomingEventCard.astro` (the homepage's "Next Up" widget and upcoming-events list) both rendered `event.layout` raw. Both now route through `displayLayoutName()`.

**Calendar page now has a Calendar-grid view alongside the existing List view.** A List/Calendar toggle sits above the page; List is the pre-existing Upcoming/Past Events layout (already fully supporting backdated events — `getEvents()` has no date filter and the page already split events into Upcoming/Past sections before this change). Calendar is a new month-grid: Prev/Next/Today navigation (Prev has no lower bound, so past months — and their backdated events — are just as reachable as future ones), days with an event highlighted and labeled with that event's circuit name, and clicking a day opens a detail panel below the grid with that day's full event info (circuit, layout, format). Client-side only, driven by a lean JSON payload (date/circuit/logo/layout/format) serialized into a `data-events` attribute on the page (same JSON-in-a-data-attribute convention the Incident Report page already uses) — no extra query.

## In-sim session times, a real Layout dropdown, a Team Standings Overall/Delta toggle, and the Unassigned-filter bug (v0.30)

**Events can now store an in-sim time per session, alongside the existing (real-world) start time.** `events` gained five nullable `time` columns, `*_sim_time` for each of practice/qualifying/race1/race2/race3 (`0023_event_sim_times.sql`). This is the simulated clock time iRacing is set to for that session — useful for planning around dawn/dusk/night conditions — and is entirely separate from the start time people actually need to show up at. The admin event form's `EventSessionFields.astro` gained a second time input per session ("In-sim time"), and every place a session's start time already renders now shows the in-sim time alongside it when set: `EventDetailCard.astro` (Calendar page) as its own "In-sim ..." line, and `UpcomingEventCard.astro` (homepage "Next Up" widget) as a "(in-sim ...)" suffix. `SessionSummary`/`getEventSessions()` in `eventFormatting.ts` carry the new `simTime` field through.

**Creating/editing an event now picks its layout from the actual list of layouts, instead of typing free text.** The admin event form's Layout field was a plain text input with no validation against what `circuit_layouts` actually contains — easy to typo or drift out of sync with the circuit's real layouts. It's now a `<select>` populated from every `circuit_layouts` row, client-side filtered to just the layouts belonging to whichever circuit is currently selected (a small script keyed off each `<option>`'s `data-circuit-id`, re-running on Circuit change; picking a new circuit that invalidates the current layout resets it to "Not set" rather than silently submitting a mismatched pair).

**Fixed the remaining white-background dropdowns on the event edit page.** Every native `<select>` on `/admin/events/[id]` — Circuit, the new Layout select, Format, and Weather (×5, one per session) — now sets `class="bg-brand-ink text-white"` on each `<option>`, the same fix already used elsewhere (e.g. Team Standings' season picker) for native dropdown lists otherwise rendering with the browser's default light background against this site's dark theme. Note: a few other admin pages (`admin/teams/[id]`, `admin/users`, `admin/drivers/[id]`) still have this same issue on their own dropdowns — out of scope for this pass since it was specifically the events edit page that was called out, but worth a follow-up sweep.

**Team Standings gained an Overall/Delta toggle**, same pattern as the driver Standings page's Overall/Per Class toggle. Overall (the existing behavior, still the default) sums each team's top-2-scorers-per-race totals across every class combined. Delta scopes that same competition to just Delta-class rows and ranks/sums by each row's full total points (Delta's own class_points bonus included) — matching how the news recap's Delta Team Scoring Breakdown (v0.26) and `topTeamDelta` figure already treat Delta as its own team competition. `computeTeamSeasonStandings()` in `src/lib/results.ts` gained an optional `classId` parameter: omitted, it behaves exactly as before; passed Delta's class id, it re-derives per-class ranking/adjustments (mirroring `computeSeasonStandings()`'s own per-class logic) before feeding the same drop-week finalization every other standings view uses. The roster-detail panel that appears when you expand a team row keeps showing each driver's *overall* championship position regardless of which toggle is active — that's a deliberate choice, not a bug, since a driver's overall standing doesn't change based on which team competition you're viewing.

**Fixed the Roster page's "Unassigned" filter showing nobody.** Root cause: `DriverRow.astro` set `data-unassigned={driver.car_number === null && driver.appearances > 0}` — a raw JS boolean. Astro has special-case handling for boolean attribute values: `true` renders as a bare attribute with no value, and `false` omits the attribute entirely, neither of which is the literal string `"true"` the filter script's `dataset.unassigned === 'true'` check was looking for, so the check always failed. Fixed by wrapping the expression in `String(...)`, matching this codebase's existing convention for any data-attribute that needs a boolean check in client JS (already used elsewhere for this same reason).

## Admin-managed page banners, per-season logos, a full admin dashboard, and a handful of site-wide polish fixes (v0.31)

**Every page can now have its own admin-managed header banner image**, from a new `/admin/page-banners` screen — new `page_banners` table (`page_key` → `image_url`, 0024_season_logos_and_page_banners.sql) plus a new public `banners` Storage bucket. `PageBanner.astro` is a small reusable component that fills the empty band between the sticky nav and each page's own `<h1>` with the configured image (fading to solid black by the bottom of that band so it blends into the rest of the page rather than hard-cutting off); a page with nothing configured just renders that space plain black, same as always. Wired into Standings, Team Standings, Roster, Calendar, Champions, Teams, News, Circuits, and Race Results. **Scoped to top-level/static pages only** — dynamic detail pages (an individual race result, an individual news post) don't have one stable "page" to hang a banner off of, so they're left out; see `src/lib/pageBanners.ts` for the full list and that reasoning.

**The homepage hero background is the same feature, special-cased.** It's the "Home" entry in `/admin/page-banners`, rendered as the full hero section (not the thinner strip every other page gets) with the existing dark overlay for text legibility. This replaces the old `public/images/hero-bg.jpg` static-file convention entirely. **The default blue radial gradient that used to always show behind the headline is gone** — with no image configured, the hero is now just plain black (this site's actual background color), not a placeholder gradient.

**Seasons can now have their own logo**, from a new `/admin/seasons` screen — ATC swaps up its branding a bit every season, and this is where that now shows: next to the season picker on Standings/Team Standings/Race Results (beside the `<select>` itself, not inside its `<option>`s — plain HTML can't put an image inside an option), and next to the season name on every Champions card. `seasons.logo_url` already existed as a column (0001_init.sql) but had no write policy at all — 0024 adds `admin update seasons`, and a new `updateSeasonLogo()` in `src/lib/supabase.ts` is the only field it's editable through.

**Every admin section is now reachable from the admin dashboard.** `/admin` was missing cards for Champions, Car Logos, and Penalty Offenses (they were only ever reachable via the sub-nav or a direct URL) — the dashboard's card list is now kept in sync with `AdminLayout`'s own nav links, plus cards for the two new sections above.

**The homepage tagline now reads "*iRacing's premier North American touring car series*"** (italicized), replacing "The premier North American touring car series on iRacing." — leads with the platform, matches how Logan phrased it.

**Fixed the one place a non-brand blue was actually rendering.** Every button/link on the site already fills with `brand.primary` (#4369F5) — sampled directly from the real logo files, confirmed via pixel-check, so there was no mismatch to find there. The one real gap: none of the site's buttons/links set their own focus style, so clicking or tabbing to one showed the *browser's* own default focus ring instead — a distinctly different, lighter blue than the site's actual color. `src/styles/global.css` now sets a global `:focus-visible` outline in `brand.primary` instead of leaving that to the browser default.

## Season logo placement, photographer credit, admin-dashboard link, photo album links, and search bars everywhere (v0.32)

**Season logos moved to the right of the season `<select>`**, not the left, on Standings/Team Standings/Race Results — purely a position swap from v0.31, no behavior change.

**The homepage hero banner now fades to black at the bottom** instead of a flat dark overlay, matching the same top-to-bottom gradient `PageBanner.astro` already uses on every other page's header banner (see v0.31). Homepage only, per Logan — every other page's banner already worked this way.

**Footer additions:** an "Admin Dashboard" link (next to Discord/Redbubble), visible only via `isAdminView()` — same admin-gated pattern used elsewhere, so it also respects the "View as Visitor" preview toggle right next to it. And a photographer credit line — "Photos by Jason Lin; All Rights Reserved; Glass & Steel Photography and Design" — with the studio name linking out to his Flickr (`https://www.flickr.com/people/200558998@N08/`).

**News post previews no longer show the author byline**, and now show that post's season logo next to its season tag (best-effort matched by name — `news_posts.season_label` is freeform text, not a foreign key to `seasons`, per its own doc comment, so a label that doesn't exactly match a season's name just shows no logo). Applied to `NewsCard.astro` (the homepage's Latest News and the `/news` grid) and, for the season-logo half only, the full post page's own season tag too — the author byline stays there since that page isn't a "preview."

**Race results can now link to that race's photo album.** A fourth per-race external link alongside iRacing Results/Download Replay/Watch Broadcast (`race_links.photo_album_url`, 0025_race_links_photo_album.sql) — same admin-editable "Edit links" form on `/results/[subsessionId]` as the other three.

**Search bars added to Circuits, Teams, and Roster.** All three are plain client-side substring filters (no page reload, no extra query) over data already on the page:
- **Circuits** (`/circuits`) matches a circuit's name or any of its layouts' display names.
- **Teams** (`/teams`) matches a team's name or any current roster member's name (`drivers.team_id` — the same "current team" a driver's own profile shows — not the season-scoped `team_rosters` table).
- **Roster** (`/roster`) matches a driver's name, car number, or team name, folded into the page's existing Active/Inactive/class filter script rather than a separate mechanism.

Each shows a "No results." message when the search excludes everything, same wording the rest of the site already uses for a genuinely empty list.

**Seasons can now be marked current from the admin.** `seasons.is_current` (the season every picker on the site defaults to) had no admin write path at all before this — 0024's `admin update seasons` policy existed, but nothing in the app actually flipped the flag. New `setCurrentSeason()` in `src/lib/supabase.ts` clears the old current season, then sets the new one (two requests, not one — `seasons` has a partial unique index allowing only one `is_current=true` row at a time, so the old one has to go false before the new one can go true). `/admin/seasons` gained a "Set as Current" button per season (hidden on the one already current), and the admin dashboard now shows the current season's name up top and on the Seasons card, so it's visible at a glance without opening that page.

## A universal Driver Stats history tab, and a team-standings drop-week bug fix (v0.33)

**New "Driver Stats" tab on the history sub-nav** (`/driver-stats`, alongside Champions/Standings/Team Standings/Race Results/Circuits) — a career-wide table of every driver who has ever scored a point in Alpha Touring Challenge, combining every class and every season they've ever raced in: championships, wins, poles, podiums, top 5s, top 10s, laps, laps led, career CPI (corners per incident), starts, and appearances. There's no stored table behind this — `computeDriverCareerStats()` in `src/lib/results.ts` derives the whole list fresh from `race_scores` on every page load by looping every championship season's already-existing standings computations (`computeSeasonStandings` per class, `computeOverallSeasonStandings`, `getSeasonDriverExtendedStats`, `computeTeamSeasonStandings` for both the overall and Delta team competitions, and `getSeasonCarTeamStats`) and folding the results together per driver. That also means a brand-new driver just appears here automatically the moment their first race is scored — nothing to backfill or refresh.

Career CPI can't just be an average of each season's own CPI (that would over-weight a low-incident season's ratio) — `DriverSeasonExtendedStats` gained a new `totalCorners` field (the raw corners-navigated numerator `cornersPerIncident` was already dividing by), so the career figure is a true `sum(totalCorners) / sum(incidents)` computed once across every season, not an average of ratios.

**Click a driver to expand a season-by-season table** — one row per season they raced, each showing that season's own stats plus the car(s) and team(s) they raced under, and their final position in that season's class standings, overall (every class combined) standings, team standings, and Delta's own separate team standings (shows "—" when a driver's team had no Delta entry that season). Same click-to-expand `data-expand-row`/`data-detail-row` pattern the Standings and Race Results pages already use. A search box filters the career table by driver name.

**Bug fix: team standings were incorrectly dropping worst rounds.** `computeTeamSeasonStandings` had been applying the same "drop the worst `BASELINE_DROP_WEEKS + season.extra_drop_weeks` rounds" rule the driver championships use (`finalizeStandings`) — but per Logan, drop weeks are a driver-standings-only rule and should never have applied to the team championship. Every round a team scored in now counts toward its total, full stop. This was a genuine bug, not a design choice that changed; there was never a case where dropping team rounds was intentional.

## Fixed: Driver Stats hitting Cloudflare's "too many subrequests" error (v0.34)

The Driver Stats page (v0.33) worked locally but failed in production with `Too many subrequests by single Worker invocation`. The cause: `computeDriverCareerStats()` was looping every championship season and, for each one, calling the ordinary single-season standings/stats functions completely unshared — `computeSeasonStandings` (once per class), `computeOverallSeasonStandings`, `getSeasonDriverExtendedStats`, `computeTeamSeasonStandings` (overall + Delta), and `getSeasonCarTeamStats`. Every one of those independently fetches its own copy of the same season-wide penalty/results context (`getSeasonOverallContext`, itself ~5 queries) plus its own copies of season-independent lookups (drivers, teams, car logos, team season logos, circuits, circuit layouts) — so a single page load was issuing on the order of 50+ database requests *per season*. With more than a handful of championship seasons on file, that blew straight through Cloudflare Workers' per-request subrequest limit.

The fix shares all of that instead of re-fetching it:
- `getSeasonOverallContext`, `computeSeasonStandings`, `computeOverallSeasonStandings`, `computeTeamSeasonStandings`, and `getSeasonDriverExtendedStats` all gained an optional "precomputed context" parameter — when a caller already has the season's shared penalty context (or its own drivers/teams/car-logos/season-logos/circuits lookups), it can hand them in and skip the fetch entirely. Every existing single-season caller (Standings, Team Standings, Champions, Race Results) omits these and behaves exactly as before — this was purely additive.
- `computeSeasonStandings` and `computeTeamSeasonStandings`'s per-class branch no longer issue their own separate `curated_race_results` fetch — both now just reuse the season-wide context's copy, which already covers every subsession the season had (a strict superset of what either needed).
- `getSeasonDriverExtendedStats` no longer issues its own second `curated_rounds` fetch — the season-wide context now exposes the same rows it already had.
- New `getRaceScoresForSeasonAllClasses()` fetches every class's `race_scores` rows for a season in one query instead of one query per class.
- `computeDriverCareerStats()` now fetches every season-independent lookup (drivers, exhibition rounds, teams, car logos, team season logos, circuits, circuit layouts) exactly once for the whole run, and for each season fetches the shared penalty context and all-classes race scores exactly once, handing both to every per-season computation that needs them.

Net effect: roughly 50+ requests per season down to about 7, regardless of how many classes or team-competition views a season needs — and that number stays flat as more seasons get added in the future, rather than growing with every one of the several functions this page calls. No output changed; this is purely a fetch-sharing optimization, verified by re-reading every touched function to confirm each still computes the exact same numbers, just from data it already had on hand instead of asking for it again.

## Fixed (for real this time): Driver Stats still hitting the subrequest limit (v0.35)

v0.34's fix shared each season's fetches across that season's several computations, but still issued that shared fetch ONCE PER SEASON — an error log from production confirmed it: `computeSeasonStandings` was still calling `getRaceScoresForSeasonClass` directly, because a class with zero rows that season made `computeDriverCareerStats` pass `undefined` (not an empty array) as the "already fetched" argument, which fell through to a live fetch instead of being recognized as "already resolved, empty." Fixing just that one bug would have helped, but the deeper problem was the whole design: even a fully-shared "1 fetch per season" still adds up to more requests than Cloudflare allows once there are more than a handful of seasons — sharing within a season was never going to be enough on its own.

This version removes per-season fetching entirely. `computeDriverCareerStats()` now bulk-fetches every season's data in a small, FIXED number of queries — regardless of how many championship seasons exist:
- One query for every class's `race_scores` rows across every championship season at once (new `getRaceScoresForSeasonsBulk()`), instead of one query per class per season.
- One query each for `curated_race_results`, penalties, average/best-lap data, and `curated_rounds.layout`, across the full union of every subsession any of those seasons touched — instead of one set of those per season. (The curated_race_results and lap-data queries are further merged into a single combined query for this path specifically, with a safe fallback to the original two separate isolated queries if that combined query ever fails.)
- Every season-independent lookup (drivers, teams, car logos, season logos, circuits, circuit layouts, exhibition rounds) — still fetched exactly once, as in v0.34.

`getSeasonOverallContext` — the shared "how do this season's penalties affect standings" computation every other function on this page leans on — got split into a fetch half (unchanged, still what every single-season page uses) and a new pure `buildSeasonOverallContext()` half that takes already-fetched rows and does no querying at all. The per-season loop now calls only the pure half, on an in-memory slice of the bulk data filtered down to that one season — zero network calls anywhere inside the loop, for any number of seasons.

Bulk-fetching this much data raised a separate correctness risk: Supabase's hosted API caps a single response at 1000 rows by default, and would silently truncate a large result instead of erroring — quietly wrong career stats being worse than a loud failure. New `restGetAll()` in `src/lib/supabase.ts` pages through PostgREST's `Range` header until a request comes back short, and now backs every query in this path that could plausibly exceed that cap (plus `getAllRounds()`, `getPenaltiesForSubsessions()`, and a couple of others already shared with the rest of the app) — with an explicit, unique sort order added to each newly-paginated query so results stay consistent across pages. For any query that stays under 1000 rows (true of every other page on the site), this behaves exactly like the plain `restGet()` it replaced — one request, no change in behavior.

Net effect: the total request count for this whole page is now a small constant (global lookups plus a handful of paginated bulk queries) that does not grow with the number of classes, team-competition views, or per-season function calls — the only thing that can still add requests is the total amount of race data across the league's entire history crossing another 1000-row page boundary, which grows far more slowly than "6 functions × N seasons" ever did.

## Fixed: sortable tables silently breaking their own expand/collapse rows (v0.36)

Once Driver Stats was actually loading, its expandable rows didn't work: clicking a driver did nothing, and clicking again would seemingly collapse (and a third click, re-expand) a completely different row below it. Root cause: `src/scripts/sortable-table.ts`'s click-to-sort behavior re-sorts a table by moving each `<tr>` around, but it was treating every direct row the same — including a `[data-detail-row]` (the hidden expandable panel that sits right after its own `[data-expand-row]` summary row). A detail row never matches any column's sort key, so every table's detail rows would sort to one end as a group the moment any column header got clicked, getting separated from the summary rows they belong to. From then on, a summary row's very-next-sibling (what the expand/collapse code reads) was some unrelated row — clicking to expand silently touched the wrong row's hidden class instead. This wasn't new to Driver Stats — Standings and Team Standings combine the same two features (sortable columns + expandable rows) and had the identical latent bug, just apparently never triggered/noticed there before.

Fixed in the one shared script: sorting now treats a `[data-expand-row]` and its immediately-following `[data-detail-row]` as one unit that always moves together, so they can never end up on opposite sides of a sort. A table with no detail rows at all (every other sortable table on the site — Roster, Teams, etc.) is completely unaffected, since every row there is just its own one-row "unit" either way.

## Loading feedback for every server round trip, and a redesigned homepage (v0.37)

**Loading feedback.** The site used to give zero visual acknowledgment between clicking something and the new page/data showing up — on a slow connection it just looked hung. Three additions, all global rather than per-page:

- A CSS `:active` press state (`src/styles/global.css`) on every link, button, select, and `[role="button"]` (which covers the sortable-table column headers too) — fires instantly on mousedown/touch, before any network activity, so a click always visibly registers.
- A new shared script, `src/scripts/pageProgress.ts`, adds a thin animated progress bar to the top of the viewport for the duration of any navigation. It's wired to Astro's `<ViewTransitions />` router lifecycle (`astro:before-preparation` shows it, `astro:page-load` hides it) so it covers every soft navigation on the site — including season/class filter dropdowns on Standings and Team Standings, which `onchange="this.form.submit()"` into a GET navigation the router intercepts the same as a link click. `window.beforeunload` covers the remaining hard-navigation cases (external links, anything marked `data-astro-reload`). A 15-second safety timeout clears the bar if a request ever hangs or fails silently, and the whole thing is a no-op version (no animation, an instant static state instead) under `prefers-reduced-motion`.
- `src/scripts/hard-form-submit.ts` (every admin `POST` form) calls the same `showProgress()`/`hideProgress()` directly around its own `fetch()`, since that request is deliberately built to bypass the router entirely and so is invisible to the hook above. The submit button's own label also swaps to "Working…" for the duration, since it's the one element the person actually clicked.

Deliberately **not** wired up anywhere purely client-side — the search-box filters (Roster, Teams, Circuits, Driver Stats, and the new homepage standings widget's tab switcher) and sortable-table column clicks never touch the network, so nothing shows for those; adding a loading indicator there would be actively misleading.

**Homepage redesign.** Per Logan's request: the old hero paired a big headline/tagline/"Sign Up" button on the left with Upcoming Events on the right. The left side is now the most recent news story instead — Sign Up isn't lost, it's just no longer duplicated here on top of the Discord button already in the top nav and footer on every page. A small heading-only title stays above the row purely so the page still has a real `<h1>`. Below that row: remaining news stories on the left, and a new **Standings** widget on the right — top 5, with a click-to-switch tab for Overall / each of the 3 classes / Rookies (Rookies is the Overall standings filtered to `drivers.is_rookie`, re-ranked 1..N among just themselves). All 5 views are computed server-side up front and toggled client-side (no server round trip per tab, consistent with the loading-feedback rule above) — the season-wide penalty context (`getSeasonOverallContext`, now exported for this reason) is fetched once and shared across all 4 `compute*Standings` calls that need it, rather than once per tab.

Below the standings row, a new **Featured Broadcast** section embeds a YouTube video/livestream — full width, only rendered when a URL is actually configured. That URL is a new admin-managed setting, `/admin/site-settings` (new generic `site_settings` key/value table, `0026_site_settings.sql`, same public-read/admin-write RLS shape as `page_banners`) rather than a page-specific table, since it's a single site-wide value with no natural table of its own. Pasting a normal YouTube watch/share/live URL is enough — `src/lib/siteSettings.ts` converts it to the embed form and rejects anything it doesn't recognize as YouTube.

## Homepage/nav/history cleanup round (v0.38)

A round of follow-up polish on the v0.37 redesign, plus a few unrelated site-wide tweaks bundled in since they landed together:

- The featured "Recent News" story now uses the same image-top/text-below card layout as every other news card, instead of the wider image-left/text-right treatment. "More News" dropped its own heading and is now a plain 6-row text list (title + date, no images, "View all" link underneath) directly below the featured story. A stray "Alpha Touring Challenge" subtitle that had been left above the hero row is gone — the "Recent News" label is now the page's actual `<h1>` instead (visually unchanged, just the correct semantic element for accessibility/SEO).
- The homepage **Standings** widget: dropped the little (nonexistent) profile-picture circles, and each driver row now shows their car number, primary car logo, and primary team logo after their name instead — same per-driver data `/standings` already shows (`getSeasonCarTeamStats`), just the top (most-raced) car/team only, to keep each row compact. Swapped the Rookies tab out (pulled back until there's more rookie-specific logic behind it) for two new team-standings tabs, **Alpha Team** and **Delta Team** — each class's own separate team competition (see `computeTeamSeasonStandings`' `classId` param), alongside the existing Overall/Alpha/Gamma/Delta driver tabs.
- Top nav reordered to Home, Calendar, News, Drivers, Teams, History, Rulebook — "Roster" is now labeled "Drivers" (same `/roster` page, label only). The nav's "Discord" button is now labeled "Sign-Up" (still links to Discord, same as the footer's own Discord link).
- The History sub-nav (`HistoryTabs.astro`, shared by Champions/Standings/Team Standings/Race Results/Driver Stats/Circuits) is now ordered Champions, Race Results, Standings, Team Standings, Driver Stats, Team Stats, Hall of Fame, Circuits — with two new placeholder pages, `/team-stats` and `/hall-of-fame` ("Coming soon" for now, both already wired into `/admin/page-banners` for whenever there's real content to build there).
- The footer now has a public "Admin Login" link (`/admin/login`) above the existing "Admin Dashboard" link — the dashboard link only ever showed for someone already signed in as admin; the login link is visible to everyone, so an admin doesn't need to already know the URL to sign in from a fresh browser.

## Timezone-aware times, a homepage standings fix, Hall of Fame, Team Stats, and more (v0.39)

A round driven entirely by a batch of user feedback — six items, each independent:

**1. Site-wide timezone-aware time display.** Every event session time (practice/qualifying/race1-3 start times) has always been entered assuming Eastern (`America/New_York`) — that's now formalized as `LEAGUE_TIME_ZONE` in a new `src/lib/timezone.ts`, with a dependency-free DST-correct converter (`zonedTimeToUtc`/`zonedTimeToUtcIso`, using `Intl.DateTimeFormat`'s own tz database rather than a new npm package). Public pages (`UpcomingEventCard.astro`, `EventDetailCard.astro`) still render a server-side ET-labeled fallback (correct even with no JS, and safe under the pages' shared edge cache — see below) but now also embed the real UTC instant in a `data-utc-time` attribute; a new global script, `src/scripts/localTime.ts`, replaces that text with the viewer's own local time (e.g. "8:00 PM PDT") once it runs. This has to happen client-side, not server-side: `/calendar` and the homepage are edge-cached for up to a few minutes and shared across every visitor who hits that cache, so a server-computed "viewer's timezone" would only ever be correct for whoever happened to generate the cached copy. On the admin side, `EventSessionFields.astro` (the form used for all 5 sessions on the event editor) now labels the input "Start time (Eastern)" and shows a live "= 8:00 PM PDT in your timezone" preview as Logan types, so entering a time doesn't require remembering to mentally convert. Out of scope, deliberately: `curated_rounds.start_time` (a real timestamptz from the iRacing results pipeline, not something Logan types in) is only ever displayed as a date, never a time-of-day, so there's nothing to convert there; the `_sim_time` fields are each event's in-sim clock, which has no real-world timezone by definition.

**2. Fixed the homepage's Alpha driver + Alpha Team standings.** Both were showing incorrect standings while every other tab (and the dedicated `/standings`/`/team-standings` pages) were correct. The homepage's standings widget shares one fetched `overallContext` across all of a season's standings calls as a subrequest-count optimization (see v0.37) — after exhaustive tracing turned up no confirmed mutation bug in that shared object, the safest fix (matching `/standings` and `/team-standings`'s own proven-correct calling convention exactly) was to stop sharing that context for the two calls that were actually wrong (`computeSeasonStandings`, `computeTeamSeasonStandings`), while leaving the Overall driver tab and the car/team-logo lookup on the shared path, since those were never reported wrong. This trades a small, bounded increase in homepage subrequests for standings guaranteed correct by construction — worth calling out that this wasn't chased down to a single confirmed root cause (no live database access from the environment that built this), just fixed by parity with the reference implementation.

**3. "Hide past events" toggle on Calendar.** A checkbox next to the List/Calendar view buttons (only shown when there are any past events at all) hides the whole Past Events section below the toggle. Defaults to unchecked (shown), doesn't persist between visits.

**4. Hall of Fame.** New `is_hall_of_fame` boolean on `drivers` (`0027_hall_of_fame.sql` — **run this migration in the Supabase SQL editor**), toggled via a new checkbox on each driver's admin edit page, right next to the existing "Rookie" one. The public `/hall-of-fame` page shows one card per member — photo, bio, and career stat line, styled like the Champions cards — and clicking a card expands a season-by-season breakdown table, styled like Driver Stats' own expandable rows. That breakdown isn't a new computation: it's `computeDriverCareerStats` (the exact same function Driver Stats uses) filtered down to just the marked members, so a Hall of Famer's numbers here can never drift from their own Driver Stats row.

**5. Fixed Driver Stats' rows not expanding on first load.** Reported: clicking a driver row right after a fresh page load did nothing, but worked after a manual reload. Root cause: Driver Stats, Standings, Team Standings, and the race-results detail table each had their own copy-pasted click-to-expand script, none of them guarded against double-binding — if `astro:page-load` ever fired twice for the same still-mounted content, every row picked up a second listener, and a single click would toggle a row open and immediately closed again in the same event (indistinguishable from "nothing happened"). Replaced all four with one new shared, delegated script, `src/scripts/expandRows.ts` (one listener on `document`, guarded the same de-dup way `sortable-table.ts` and `hard-form-submit.ts` already were) — it also means new expandable UI (Hall of Fame, Team Stats, below) gets this for free with zero setup.

**6. Team Stats.** New `/team-stats` page mirrors Driver Stats' layout and columns (Champs, Wins, Podiums, Top 5s/10s, Poles, Laps, Laps Led, CPI, Starts, Apps), search box, sortable columns, and expandable season-by-season rows — but for teams, and covering every team that's ever fielded a driver, not just the current roster. A new `computeTeamCareerStats` (`src/lib/results.ts`) computes this as a pure in-memory pivot of `computeDriverCareerStats`' own output — zero extra Supabase queries — attributing each driver-season's stat line to that driver's *primary* team that season (the same "primary team" concept Driver Stats' own Team Pos column already uses). "Championships" here means seasons a team finished 1st in the Overall Team Standings; there's no separate notion of a team-level "win" or "pole" independent of its drivers' own results in this scoring system, so those figures are a straightforward roll-up rather than a new scoring rule.

## CSV imports for Events/Circuits/News/Race Results, race-recap history everywhere, a full Calendar day view, and two subrequest-exhaustion fixes (v0.40)

**Two bug fixes, same root cause.** The homepage standings widget had started showing "No standings yet." and the Featured Broadcast YouTube embed had disappeared, despite nothing changing data-wise. Both are explained by the same failure mode that hit Driver Stats back in v0.34/v0.35: v0.39's Alpha-standings fix (item 2 above) stopped sharing `overallContext` across the homepage's 3 class + 2 team standings tabs, pushing the page's total Supabase subrequest count for one load to roughly 50-56 — plausibly over Cloudflare Workers' per-request subrequest cap. That cap is cumulative across the *whole* request regardless of try/catch boundaries, so once it's hit, every fetch still queued behind it comes back empty — including the code-sequentially-later YouTube embed fetch, in a completely unrelated code path. Fixed by introducing a *second* shared context — one for the Overall tab (unchanged from v0.39), and one new one shared across just the 3 class + 2 team tabs together (still never shared with the Overall tab's own) — cutting the homepage back down to roughly 26 subrequests, and by reordering the broadcast/hero-banner fetch to run before the standings widget as defense-in-depth. Worth being upfront: like the v0.39 fix, this is the best-reasoned diagnosis given the evidence (careful counting of every `restGet` call in each function's chain), not something confirmed against a live database — this environment has no Supabase network access to test against.

**Three straightforward CSV importers: Events, Circuits, News.** Each admin list page (`/admin/events`, `/admin/circuits`, `/admin/news`) now has a collapsible "Import from CSV" panel — download a template, upload a filled-in copy, and rows are matched to existing records (by circuit+date for Events, name for Circuits, slug for News) so a re-upload updates those rows in place instead of duplicating them. A blank cell on an update always means "leave this field as-is," never "clear it." One bad or unrecognized row is skipped rather than failing the whole batch, and the page reports back `Imported N, M skipped` after the upload. All three share the same small hand-rolled CSV parser (`src/lib/csv.ts`) the Incident Report importer (v0.16) already used.

**A fourth, much bigger one: Race Results.** Reachable from the History → Race Results tab (a new admin-only "Import Race Results from CSV" panel) or, for convenience, a link from the Calendar page's event list — both point at the same panel. Unlike the three importers above, this one writes directly into `curated_rounds`, `curated_race_results`, and `race_scores` — the tables an external iRacing-results pipeline has otherwise owned exclusively since this repo's very first migration (see `src/lib/results.ts`'s own header comment, and 0004/0014/0018's). That's a deliberate, one-off exception: asked directly whether to write into the pipeline's own tables or keep results app-owned and separate, Logan chose writing directly in — fastest to build, and every downstream feature (standings, career stats, recaps, "Race Recaps at this Layout" below) picks up an imported round automatically since it's just a normal row in the same tables a real pipeline import would produce. Meant for exhibition races or one-off events the real pipeline will never see — a round that WILL eventually show up in a real import shouldn't go through this, since the app has no way to know a synthetic round and a later real one are "the same" race.

Made safe two ways (**run `0028_manual_results_import.sql` in the Supabase SQL editor** before using this — it grants the admin write access this importer needs and adds one new small app-owned table):
- Every manually-imported round gets a *negative* `subsession_id` — real iRacing subsession_ids are always positive, so a real pipeline import can never collide with or overwrite a manually-imported one. A new `manual_result_imports` table maps each CSV's own `import_key` column (a label you choose, e.g. `exh-2026-08-09-watkinsglen`) to the synthetic id it was assigned, so re-uploading a corrected CSV with the same key fully replaces that round's results instead of duplicating them.
- Since this app has no visibility into the real pipeline's actual points-per-finish-position formula (computed entirely outside this repo), the CSV requires the scoring fields themselves — `finish_points`/`class_points`/`total_points`/`finesse_bonus`/`pole_bonus`/`points_deduction` — rather than deriving them from raw finishing position.

One firm requirement, called out in the panel's own help text: `curated_race_results` identifies a driver by their iRacing customer id, not this app's own driver record, and every reader in `results.ts` bridges the two via `drivers.iracing_cust_id` — a driver with no Customer ID set on their Roster profile can't be joined this way, so any row for them is silently skipped (with the reason logged server-side). Set that field first for anyone whose results you're backfilling. All of this logic lives in a new `src/lib/raceResultsImport.ts`, kept separate from `results.ts` (which stays read-only, per its own header) specifically so this one deliberate exception to the pipeline-ownership rule stays easy to find and easy to lift back out later if this repo ever gets real write access to the pipeline's tables some other way.

**Race-recap history in the event list.** Every event card (Calendar's List view, both upcoming and past) now has a collapsible "Race Recaps at this Layout" section — every historical round ever run at that exact circuit+layout, newest first, each one its own nested collapsible showing the same recap a linked news post already shows (top 3 per class, top rookies, fastest lap, Sublime Finesse, Naked Aggression, team points). The *list* of which rounds match a card's layout is cheap and computed once for the whole page up front (`findRoundsForLayout`, reusing the same round→layout resolution `results.ts` already had); the actual recap *content* is deliberately deferred to a new `/api/round-recap/[subsessionId]` JSON endpoint, fetched client-side only the moment a visitor actually expands a given round, and cached after that. Computing every matching round's full recap eagerly for every card on the page would have risked exactly the same subrequest-exhaustion failure described above — a circuit can easily have been raced dozens of times across the site's history.

**Full event details on the Calendar day view.** Clicking a day in the Calendar grid used to show an abbreviated summary built from a separate, leaner data path than the List view. It now shows exactly what List view shows — literally the same DOM: the day-click handler clones the already-rendered `EventDetailCard` node(s) straight out of the List view's markup (tagged via a new `data-event-date` attribute) rather than maintaining a second parallel rendering path, so the two views can never drift out of sync. The cloned cards' own race-recap collapsibles work immediately too, guarded with a `WeakSet` rather than a dataset flag — `cloneNode` copies `data-*` attributes but never copies event listeners, so a dataset-flag guard would have wrongly treated every clone as "already bound" while leaving it permanently inert.

## Re-importing the roster later

Whenever the roster spreadsheet changes:
```
python3 supabase/seed/generate_seed.py path/to/roster.xlsx
```
This regenerates `supabase/seed/seed_teams.sql` and `seed_drivers.sql`. Re-run them in the Supabase SQL editor — both are upserts, safe to run repeatedly.

## Known placeholders — swap these out

- **Colors** (`tailwind.config.mjs`): resolved — using the confirmed brand hex codes (`#F5426E` pink, `#4369F5` blue, `#F5C642` gold).
- **Fonts**: Teko (display) + Roboto (body) load from Google Fonts, no files needed. Teko is standing in for Cuatra pending a confirmed commercial license — see the note in `src/styles/global.css` for why a "free for personal use" font license likely doesn't cover a public organization's branding even if it's non-profit (that's not legal advice — check the license text that came with your Cuatra files, or ask the foundry, if you want certainty).
- **Logo**: resolved — the real ATC18 logo (`public/logos/atc18-white.png` for the dark nav, `atc18-black.png` also included for any light-background use) pulled from `E:\ATC Media\Logos` on your machine.
- **News cover image**: resolved — `public/images/news/swirydowicz-champion-atc17.jpg`, referenced directly in `seed_news.sql` as a static site asset (no Supabase Storage needed for this one).
- **Favicon**: resolved — generated from your `favicon.png` into `public/favicon.ico` (16/32/48px), `public/favicon-192.png`, and `public/apple-touch-icon.png` (180px), wired up in `src/layouts/Layout.astro`.
- **Circuit logos**: still a placeholder — no circuit has a logo uploaded yet, so cards fall back to a blank square. Upload one per circuit from `/admin/circuits` whenever you have them; nothing else needs to change.
- **Homepage background image**: no longer a static file — as of v0.31 it's admin-managed from `/admin/page-banners` (the "Home" entry), same as every other page's header banner. Nothing uploaded yet, so the hero currently renders plain black; upload one whenever you have it, no code change needed. Sizing guidance:
  - **Dimensions**: 2560×1440px minimum (16:9). The image is stretched to `cover` behind the hero text, so on very wide monitors it can be cropped horizontally — a subject that's reasonably centered (not off in a corner) holds up best across screen sizes.
  - **Format**: JPG or WEBP, optimized to roughly 300–500KB (this loads on every visit to `/`, so it's worth compressing — TinyPNG or Squoosh both work well).
  - **Contrast/legibility**: a dark, semi-transparent overlay (`bg-brand-ink/70`) is already layered on top of whatever image you add, so the white headline text stays readable regardless — but a photo that's already darker or lower-contrast in its upper-middle area (where the headline sits) will look best.

No known placeholders remain for v0.2. The homepage background image and circuit logos are the only open items as of v0.4.

## A note on this project's first build

This was scaffolded in a sandboxed session without access to the npm registry, PyPI, or Supabase's API (only GitHub was reachable), so none of it could be `npm install`ed or run in that session — every file was hand-written instead of generated by `npm create astro` and friends. It's worth running `npm install && npm run build` yourself as a first check before deploying, in case anything needs a small fix.

---
// Penalty Offenses was merged into Site Properties (which also absorbed
// Site Settings, Page Banners, and Car Logos) — kept as a redirect for
// anyone with an old bookmark/link.
export const prerender = false;
return Astro.redirect('/admin/site-properties', 301);
---

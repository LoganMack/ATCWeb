---
// Page Banners was merged into Site Properties (which also absorbed Site
// Settings) — kept as a redirect for anyone with an old bookmark/link.
export const prerender = false;
return Astro.redirect('/admin/site-properties', 301);
---
